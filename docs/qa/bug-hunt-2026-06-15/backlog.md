# Prioritized Backlog — New_Bazi Bug-Hunt Sprint

Bands are P0 (ship-blockers / data-integrity / security), P1 (broken user-facing behavior), P2 (boundary / robustness), P3 (polish). **Within each band, rows are ordered strictly by the supplied score** (severity × user-value × feature-completion). Score is shown to make the ordering auditable.

## P0 — Critical (anti-reification, security, ship-blockers)

| Rank | ID | Title | Area | Type | UserImpact | Score | Why this rank | Suggested fix direction |
|---|---|---|---|---|---|---|---|---|
| 1 | BIRTH-TIME-01 | Unknown-time honesty has no local backstop; 12:00 ascendant/houses/hour can show as fact | Unknown-time / anti-reification | contract-violation | high | 14 | Directly violates the enforced product principle; FR-015 cannot be called complete until the honesty guarantee is local, not engine-dependent | When `input.timeKnown===false`, force `ascendant/houses/hour` to missing-state in the normalizer regardless of `provisional_fields`; add a test pairing `timeKnown:false` with a raw response that **lacks** `provisional_fields` (`fufireNormalizer.ts:229,322-347,440,486`) |
| 2 | SEC-RATELIMIT-01 | Expensive compute routes are unauth + unthrottled — unbounded Gemini/FuFirE cost | BFF / Gemini / synastry / daily | security | high | 14 | Denial-of-wallet: anonymous POSTs drive billable Gemini tokens + FuFirE quota (synastry=2, daily=2, profile≤5 upstream calls each) | Add `express-rate-limit` (IP + global) on `/api/gemini/reading`, `/api/azodiac/{synastry,daily,profile}`; consider auth gate on Gemini (`app.ts:384,408,443,496`) |
| 3 | DB-SECURITY-07 | Service-role key bypasses RLS for all BFF queries; isolation rests on never forgetting `.eq('user_id')` | Multi-tenant isolation | security | high | 12 | Structural: today correct, but one future query without the owner filter leaks/mutates cross-tenant rows with zero DB backstop and no test catches it | Add a thin query helper that **requires** a userId filter, or run user-scoped queries through a JWT-bound (anon-key + RLS) client; add a real-RLS integration test (`supabase.ts:13-21`; `app.ts:527-617`) |
| 4 | FUFIRE-ORCH-01 | `Promise.all` fails the whole profile when one orchestrated section errors | Profile orchestration | boundary | high | 12 | One transient 429/503 on any of 4 parallel calls 502s the entire profile, defeating the normalizer's per-section degradation — breaks the core read for a recoverable blip | Switch `Promise.all`→`Promise.allSettled`; assign only fulfilled sections; let normalizer render missing-state for the failed one (`profileService.ts:77`) |
| 5 | ASTRO-MATH-01 | Local fallback fabricates retrograde flags + planet longitudes, shown as real positions | Western local fallback | contract-violation | high | 12 | Anti-reification violation: invented `marsLong = sunLong-18+0.524*days`, retrograde = `sin(idx)`, surfaced as measured `retrograde`/`degree`/`house` | Gate the local-fallback planets/retrograde behind an explicit "simulated — not measured" label, or remove fabricated metrics; never emit `retrograde`/sub-degree as fact (`astrology.ts:300-379`) |
| 6 | FE-WUXING-01 | Present-but-empty WuXing renders five fake 0% bars (B-002 class) | Wu-Xing | bug | high | 12 | Same fake-zero anti-reification bug the team already fixed for Fusion, unfixed for WuXing — fabricated "everything deficient" reading | Treat `weightTotal===0` as not-available; gate `WuXingDetail` on real distribution, not just `Boolean(raw.wuxing)` (`fufireNormalizer.ts:530,557,565`; `WuXingDetail.tsx:146`) |
| 7 | DEPLOY-BOOT-02 | Boot failure exits 0, defeating ON_FAILURE restart | Deploy | bug | high | 12 | A crashed boot (e.g. missing dist) throws, skips `listen`, process exits 0 → orchestrator never restarts; silent dead service in prod | Add `process.exit(1)` in `startServer().catch` (`server.ts:44-46`) |
| 8 | DB-INTEGRITY-01 / API-PERSIST-02 / SEC-JSONB-02 | birth_data stored RAW from req.body, not `validation.value` — uncanonical jsonb + arbitrary extra keys | Profile/partner persistence | bug | medium | 10 | Validator normalization (trim, gender default, coerced lat/lon, dropped unknown keys) is discarded; round-trip drift + unbounded jsonb breaks the documented contract | Insert `validation.value`, not the raw body, on both POST routes (`app.ts:556,600`) |

## P1 — High (broken user-facing behavior / feature-completion)

| Rank | ID | Title | Area | Type | UserImpact | Score | Why this rank | Suggested fix direction |
|---|---|---|---|---|---|---|---|---|
| 9 | ASTRO-MATH-02 | Ascendant/houses from a 15°/hour-from-6AM approximation ignoring latitude & sidereal time | Western asc/houses | bug | high | 12 | Can be wrong by 90°+ (whole zodiac quadrants); every house assignment inherits the error → wrong chart shown confidently (FR-009) | Compute ascendant from LST + latitude + obliquity, or mark the local path "approximate" and suppress house assignments (`astrology.ts:342-348,364-368`) |
| 10 | FE-BAZI-METRIC-01 | BaZiDetail hardcodes fabricated "1.5x Wichtung" metric; scanners skip detail components | BaZi detail | contract-violation | medium | 10 | Fabricated metric (not from any engine value) shown as fact; wording-honesty regex never scans detail components → silent anti-reification leak (FR-010) | Remove the literal or bind it to a real viewModel value; extend `wordingHonesty.test.ts` to scan detail components (`BaZiDetail.tsx:145`) |
| 11 | FE-WEST-HOUSE-01 | Planet table shows "Haus 0" when house unknown | Western | bug | medium | 10 | "Haus 0" reads as a real house; table sits outside the `housesAvailable` guard → unknown rendered as fact | Render em-dash/"unbekannt" for house===0; move table inside the houses-available guard (`WesternAstrology.tsx:104`; `fufireNormalizer.ts:267`) |
| 12 | GAP-FR008-01 | FR-008/B-005 PDF report still bare `window.print()` | PDF Report | gap | medium | 8 | Largest unbuilt in-scope user-facing feature; "Bericht herunterladen" produces an unstructured browser print, not the chaptered report users expect | Add a report template + HTML-to-PDF pipeline (Western/BaZi/WuXing/Fusion/disclaimer chapters) (`Overview.tsx:203`) |
| 13 | FE-A11Y-ACCOUNT-01 | AccountMenu a11y gaps + ExplanationLayer no focus-trap/restore + magic-link no aria-live | Account menu | ux | medium | 8 | Keyboard/screen-reader users can't operate the menu or escape the layer; magic-link errors invisible — blocks the just-shipped FR-007 layer for a11y users | Add `aria-expanded/haspopup`, Escape + outside-close, focus trap/restore, `aria-live` on magic-link status (`AccountMenu.tsx:110-125,166-171`; `ExplanationLayer.tsx:71-82`) |
| 14 | CONFIG-VITE-03 | VITE_SUPABASE_* build args unwired, no guard | Auth | gap | medium | 8 | If creds absent at build, `supabase=null` silently kills login with no diagnostic — a deploy-config trap that breaks the whole auth/persistence feature | Wire `build.args` in `railway.toml`; add a startup guard that fails visibly when Supabase env is missing (`Dockerfile:11-14`; `supabaseClient.ts:3-6`) |
| 15 | FUFIRE-HONESTY-02 | `signalLevel` ('spürbar') emitted while `coherenceIndex` is null | Fusion / Kohärenzindex | contract-violation | medium | 8 | User sees a confident strength bucket on top of a "Keine Kohärenz-Daten verfügbar" headline — internal contradiction the honesty principle forbids | Null/suppress `signalLevel` whenever `coherenceIndex===null`; update the pinned test (`fufireNormalizer.ts:625-642`; `realshapes.test.ts:307-316`) |
| 16 | FUFIRE-WUXING-04 | WuXing present-but-empty renders 0% distribution as 'available' | Wu Xing distribution | boundary | medium | 8 | Engine `wuxing:{}` / all-zero weights → all five "Defizit" at 0% + "dominante …-Frequenz (0%)" sentence; fabricated reading (pairs with FE-WUXING-01) | When `weightTotal===0`, set wuxing not-available (mirror Fusion's `coherenceIndex:null` pattern) (`fufireNormalizer.ts:530-588`) |
| 17 | FUFIRE-RESIL-03 | No retry/backoff on transient upstream failures | FuFirE client resilience | boundary | medium | 8 | Single fetch; one 429/503/timeout fails hard. Combined with ORCH-01, a single blip fails the whole profile | Add bounded exponential-backoff retry honoring `Retry-After` for 429/503 in `request()` (`fufireClient.ts:141-198`) |
| 18 | GEO-PROVIDER-07 | No geocoding provider fallback — single Photon outage hard-fails all place search | Geocoding resilience | gap | medium | 4 | Place entry is the first step of every flow; a komoot outage takes down the entire app entry. (`uncertain`: `fix/free-geocoding-providers` branch in flight) | Merge/finish the multi-provider branch; add a secondary geocoder + degraded path (`mapsService.ts:65,137-173`) |

## P2 — Medium (boundary / robustness / API contract)

| Rank | ID | Title | Area | Type | UserImpact | Score | Why this rank | Suggested fix direction |
|---|---|---|---|---|---|---|---|---|
| 19 | API-ERR-01 | Malformed JSON returns Express HTML 500 (a) breaking `{error,message}` contract + (b) leaking stack/file paths | API error handling | contract-violation / security | medium/low | 8/6 | Frontend loses the typed error; default handler serializes `err.stack` with full repo + node_modules paths to the client; wrong status class (should be 400) | Add a 4-arg `(err,req,res,next)` middleware that returns JSON 400 for body-parse errors and never serializes stack (`app.ts:262`) |
| 20 | API-TZ-03 | `/api/timezone` fabricates 'Etc/GMT' for null/empty/false coords (HTTP 200) | Timezone | bug | medium | 8 | Coordinates the user never supplied silently resolve to a real-looking tz instead of a missing-state error — birth-time-accuracy honesty hole | Reject null/''/non-finite lat/lon with 400 before `Number()` coercion (`app.ts:343-354`; `mapsService.ts:213-217`) |
| 21 | API-AC-04 | `/api/places/autocomplete` non-string input → unhandled TypeError 500 | Autocomplete | boundary | low | 6 | `input.trim is not a function` surfaced verbatim; a thrown internal error on bad client input | `typeof`-guard input → 400; or guard in `getAutocompletePredictions` (`app.ts:321-328`; `mapsService.ts:138`) |
| 22 | API-GEM-05 | `/api/gemini/reading` returns 502 for missing/invalid prompt after a wasted upstream call | Gemini | contract-violation | low | 6 | Client input error reported as upstream outage AND spends a real Gemini call (cost + wrong status) | Validate `prompt` is a non-empty string before calling the SDK → 400 (`app.ts:496-523`) |
| 23 | GEO-TZ-04 / API-BOUND-03 | `/api/timezone` returns 502 (server fault) for NaN / out-of-range client coords | Timezone | contract-violation / boundary | low | 6/4 | Bad client request reported as "Ortsdienst nicht verfügbar"; entry points disagree (`validateBirthInput` range-checks, this route doesn't) | NaN/range-guard lat/lon → 400 before `getTimezone` (`app.ts:343-353`; `mapsService.ts:213-223`) |
| 24 | API-GEO-02 | `/api/geocode` non-string placeId → unhandled TypeError 500 raw JS error | Geocoding | boundary | low | 4 | `placeId.startsWith is not a function` leaked; truthy non-string bypasses the falsiness guard | `typeof placeId === 'string'` guard → 400 (`app.ts:357-380`; `mapsService.ts:80-81`) |
| 25 | DB-INTEGRITY-02 | Non-atomic clear-then-insert races the one-default unique index; 23505 → opaque 502 | Default profile | boundary | low | 4 | Concurrent makeDefault:true → expected unique-conflict turned into generic 502; failed insert after clear leaves user with ZERO defaults | Wrap clear+insert in a transaction/RPC; handle 23505 / onConflict gracefully (`app.ts:547-560`) |
| 26 | DB-INTEGRITY-03 | No per-user row cap on profiles/partners | Persistence | boundary | low | 4 | Denial-of-wallet / storage abuse — unbounded rows per account, each with unbounded jsonb + label | Count-before-insert cap (e.g. N profiles/partners) → 409 (`app.ts:538-560,586-604`) |
| 27 | FUFIRE-PROV-05 | Orchestrated profile can't distinguish "all merged" from "a section silently failed" | Provenance honesty | gap | low | 4 | Blocks the *correct* ORCH-01 fix from being honest: post-`allSettled`, a failed section is labelled identically to a never-present one | Add an explicit `upstream-error` provenance status distinct from `missing`; carry it from `allSettled` rejections (`profileService.ts:78-83`; `fufireNormalizer.ts:732-765`) |
| 28 | GAP-DB-PARTNER-01 / DB-INTEGRITY-06 | `nb_partner_profiles` lacks `updated_at` + touch trigger (asymmetry vs `nb_profiles`) | Partner persistence | gap | low | 4 | Latent: once partner editing ships, no last-modified signal; GET orders by `created_at` — under-models FR-001 "partner_profiles" | Add `updated_at` + attach touch trigger; order GET by `updated_at` (`migration:18-25,40-44`) |
| 29 | ASTRO-MATH-04 | Zi-hour (23:00–23:59) day-pillar rollover not applied | BaZi day pillar | boundary | medium | 6 | Late-night births get the wrong day pillar/day master (late-Zi belongs to next day) | Roll day stem/branch forward by one when decimalHour>=23 (`astrology.ts:146-167`) |

## P3 — Low (polish / dead-or-latent code / contract niceties)

| Rank | ID | Title | Area | Type | UserImpact | Score | Why this rank | Suggested fix direction |
|---|---|---|---|---|---|---|---|---|
| 30 | API-DELETE-05 | DELETE routes always 204 even when nothing deleted | Profiles/partners delete | contract-violation | low | 6 | No-op indistinguishable from success; REST client misled (owner filter is correct) | Inspect affected-row count → 404 when zero (`app.ts:563-573,607-617`) |
| 31 | DB-INTEGRITY-04 | `label` unbounded/un-trimmed (truthiness-only on partners) | Labelling | bug | low | 6 | Megabyte/whitespace labels stored; no parity with name's 2..80 bound | Trim + length-bound label on both routes (`app.ts:539,587-590`) |
| 32 | SEC-LEAK-03 | `sendError` forwards `err.message` verbatim — non-typed throws leak internal strings | BFF error handling | security | low | 6 | Contract "never leak secrets/stack" weaker than stated for plain `Error` throws (no stack, message only) | Whitelist typed errors; map untyped throws to a generic message (`app.ts:64-71`; `mapsService.ts:182`) |
| 33 | ASTRO-MATH-05 | `compareProfiles` synastry collapses charts to hardcoded constants (B-009 legacy) | Synastry score | gap | medium | 6 | Latent: richer machinery exists and is used by the live route; this naive path is legacy/labelled but still callable | Route legacy callers to `computeInterAspects`/`compareBaziPillars`, or delete `compareProfiles` (`synastry.ts:47-69`) |
| 34 | ASTRO-MATH-07 | Local western aspects render "auf X° Orb genau" over imprecise mean longitudes | Western aspects | contract-violation | low | 6 | Overstates sub-degree precision on positions wrong by many degrees (depends on ASTRO-MATH-01) | Drop the "Orb genau" precision claim on the local path; reserve for the honest `interAspects` path (`astrology.ts:391-416`) |
| 35 | ASTRO-MATH-06 | Local WuXing renormalization dumps rounding remainder onto Earth | WuXing distribution | bug | low | 6 | Math artifact distorts the Earth element below its true share | Distribute the remainder proportionally, not all onto Earth (`astrology.ts:231-241`) |
| 36 | SEC-PLACES-04 | Open unauth/unthrottled outbound proxy to Photon | Places autocomplete | security | low | 6 | No SSRF (host hardcoded, input encoded); residual risk = free relay / OSM-etiquette abuse | Throttle (covered by SEC-RATELIMIT-01); set a proper User-Agent/quota (`app.ts:321-328`; `mapsService.ts:137-167`) |
| 37 | ASTRO-MATH-03 | `calculateSynastry` Rat/Ox bonus dead ('Ochse' vs engine 'Büffel') | Synastry animal harmony | bug | low | 6 | Latent correctness bug; `calculateSynastry` currently has no callers (dead code) | Standardize on 'Büffel'; add a test, or delete the dead function (`astrology.ts:521-531,31`) |
| 38 | API-GAP-06 | Partner routes + both DELETE routes have no frontend caller | Account / saved profiles | gap | low | 4 | 4 of 6 auth routes ship with backend+tests but no UI: no profile-delete, partner persistence dead from client | Build delete/manage UI; wire partner CRUD or scope it out explicitly (`AccountMenu.tsx`; `app.ts:575-617`) |
| 39 | BIRTH-YEAR-03 | No lower year bound — 0000/0001 & pre-1582 dates accepted and forwarded | Birth date validation | boundary | low | 4 | Typo `0190` indistinguishable from real date; pre-Gregorian dates silently wrong in ephemeris | Add a minimum-year floor (e.g. ≥1582 or engine-supported range) in `birthInputValidation.ts:80-90` |
| 40 | GEO-TZ-HIST-06 | tz resolved at 'now' with current political boundaries — historical births near moved borders get wrong tz_id | Timezone / historical | boundary | low | 4 | Off by up to a full hour for affected locations; undisclosed limit presented as authoritative | Disclose the limitation in UI; long-term, use a birth-instant-aware tz resolver (`app.ts:357-379`; `mapsService.ts:213-217`) |
| 41 | GAP-FR011-01 | Ledger marks FR-011 missing; code already implements full pair journey | Synastry (ledger accuracy) | gap | low | 4 | Planning hazard — re-doing finished work. Update the ledger, don't build | Mark FR-011 ~DONE in the ledger (`app.ts:443-481`; `synastry/PartnerJourney.tsx`) |
| 42 | GAP-LEDGER-STALE-01 | Ledger over-states open work: B-001/B-002/B-006/FR-015 DONE but listed open | Ledger accuracy | gap | low | 4 | Anyone planning from the 2026-06-11 ledger re-does merged work | Re-baseline the ledger against current `main` before sprinting |
| 43 | CONFIG-ENVDOC-04 | env drift: GOOGLE_MAPS dead, APP_URL unused, DISABLE_HMR undocumented | Config | contract-violation | low | 4 | `uncertain` — hygiene only | Prune `.env.example`; document `DISABLE_HMR` (`.env.example`; `vite.config.ts:26`) |
| 44 | DB-INTEGRITY-05 | DELETE of default profile leaves user with zero defaults | Default profile mgmt | gap | low | 2 | `uncertain` — UI assuming "exactly one default" reads zero; no reassignment | On default-delete, promote another profile to default (`app.ts:563-573`) |
| 45 | GEO-PLACEID-05 | `pl1:` placeId carries client-trusted lat/lon with no range validation | Geocode | boundary | low | 0 | `uncertain` — forgeable placeId yields out-of-range (→502) or swapped-but-in-range (wrong tz) coords | Range-validate decoded lat/lon in `decodePlaceId` (`mapsService.ts:80-92,189-196`) |
| 46 | FUFIRE-DST-06 | DST-gap 422 decode untested for `/v1/calculate/*` endpoints | DST-gap handling | boundary | low | 0 | `uncertain` — shared decode is correct, but only `/chart` has a fixture; `/v1` 422 body shape unverified | Add a `/v1/calculate/*` 422 `dst_error` fixture/test (`fufireClient.ts:178-189`; `fufirePayloadMappers.ts:140-178`) |
| 47 | FUFIRE-CFG-07 | `getFuFirePathPrefix` silently coerces unknown FUFIRE_API_VERSION to 'v1' | FuFirE config | boundary | low | 0 | `uncertain` — masks a real prefix misconfig as a 404 | Log a startup warning when FUFIRE_API_VERSION is non-empty/non-v1 and PATH_PREFIX unset (`fufireClient.ts:97-112`) |
