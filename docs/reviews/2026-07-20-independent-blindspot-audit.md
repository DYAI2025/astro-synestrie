# Independent Blind-Spot Spec Audit — Western Synastry MVP

Date: 2026-07-20 · Repo: astro-synestrie · Method: 4 independent audit lenses, then re-judged by 3 diverse skeptics with a majority rule (>=2 refutes kills).

Independence note: the 2026-07-19 reviews in this directory share an author chain with the PRD they validate. This audit was run against the repository, not against those reviews.

Calibration note: a first verification pass required UNANIMOUS survival across two skeptics and reported only 1 surviving finding. That bar was wrong — a single 'already documented' vote erased evidence-solid findings. Re-judged under a majority rule, **all 30 survived**. The numbers below come from the corrected pass.

**30 findings survived. 0 refuted.**

---

## F-01 — [unsupported-claims] (refutes 0/3)

The matrix row "Existing FuFirE Western calculation can be reused" is marked SUPPORTED, but the load-bearing precondition of the whole uncertainty feature — that FuFirE returns *different* body positions for three different submitted local times — is nowhere established. The governing request parameter, `birth_time_known`, appears ZERO times in the PRD, the plan, and both reviews. Either horn of the resulting dilemma breaks a PRD premise: (a) send `birth_time_known:false` (what the existing mapper does for approximate/unknown, src/utils/fufirePayloadMappers.ts:149) and it is unverified whether the engine honours the clock at all — if it does not, all three samples collapse to one chart and EVERY aspect becomes vacuously `stable` under plan step 7; or (b) send `birth_time_known:true` with a guessed time to force three distinct charts, in which case `precision.provisional_fields` returns `[]` (proven by src/__fixtures__/fufire/western.json → `{"birth_time_known":true,"provisional_fields":[]}` vs the unknown-time fixture's `["ascendant","houses","mc"]`) and the engine's own honesty signal — the exact mechanism src/utils/fufireNormalizer.ts:237-238 relies on — is destroyed for the new flow, so AC-004's houses/angles rule must be re-implemented from scratch in new code with no upstream backstop. Konfabulations-class: ungeprueft, used as a premise.

- **evidence** — refuted: `False`. All anchors verified verbatim: matrix row (EVIDENCE_DIALECTIC_VALIDATION.md:28, SUPPORTED, "No live request executed"); mappers.ts:149 `birth_time_known: input.timeKnown !== false`; fixtures `true/[]` vs `false/["ascendant","houses","mc"]`; normalizer:237-238; plan step 7; AC-004. `birth_time_known` is 0-hit in PRD, plan, decisions, canvas, both reviews. Dilemma holds — and birthInputValidation.ts:97 hardcodes "12:00" when timeKnown=false, strengthening horn (a).
  - *narrowed:* Keep the finding, narrow one clause. Normalizer:237-238 is `!timeKnown || provisionalFields.includes(...)` — the local `!timeKnown` term is primary and lines 232-236 explicitly designate provisional_fields as "an ADDITIONAL trigger, never the sole one." So horn (b) does not destroy "the exact mechanism relied on," nor force re-implementation "from scratch": it removes only the *engine-side* corroboration, leaving AC-004 resting entirely on the new flow's own local time-mode state (a reusable pattern, not a rewrite). Substantively unchanged: the precondition that FuFirE varies body positions with submitted clock time is untested and carried as SUPPORTED.
- **consequence** — refuted: `False`. Actionable in-scope. `birth_time_known` is absent from all four western-synastry docs; TASK-008 reuses `buildWesternPayload`, whose flag derives from `timeKnown`, and TASK-007's tests count calls but assert nothing about payload contents. The sampling flag value is an undecided, untested fork in new files. But horn (a) is false: `unknown-time/western.json` (jd_ut 2448057.9167 = submitted 12:00 Berlin, Ascendant 163.89) proves the engine honors the clock with the flag false.
  - *narrowed:* Narrower defensible version: the plan never decides what `birth_time_known` the three sample calls send, and no test pins it. Repo evidence shows the correct answer is `false` (distinct charts AND provisional_fields both survive), so there is no dilemma — but nothing enforces it. Act on it in: TASK-007 (assert captured payloads carry `birth_time_known:false` and three distinct `date` values), TASK-008 (pin the decision in the new `relationshipTransport.ts`), TASK-010 (add a degenerate-samples test so identical charts cannot yield vacuous `stable`, and enforce the houses/angles exclusion locally rather than trusting upstream `provisional_fields`).
- **coverage** — refuted: `False`. No existing mechanism closes this. Grep: birth_time_known appears 0 times in canvas/PRD/plan/decisions. The sole flag-false evidence (scripts/fufire-unknown-time-spike.mts:32-37) submits only 12:00 — indistinguishable from engine substitution. No test varies the clock with flag false; .github/workflows contains only deploy-cloudrun.yml (no test CI); src/utils/relationship/ absent. The FuFirE contract doc is prose in another repo covering only provisional_fields.
  - *narrowed:* The load-bearing precondition — that FuFirE returns different body positions for three different submitted local times under birth_time_known:false — is unverified and unnamed in all four governing docs. The only flag-false artifact (spike script, 12:00 placeholder) cannot discriminate "honors clock" from "substitutes 12:00". Drop the fufireNormalizer:237-238 sub-claim (that line already reads !timeKnown || …).

---

## F-02 — [unsupported-claims] (refutes 0/3)

EVD-004 cites "FuFirE routers/western.py" as `repository_evidence` at confidence 5/5. No FuFirE source exists in this repository (a filesystem scan finds only local tooling .py files: scripts/validate_pack.py and config/claude/lib/*.py). Half the cited evidence for a SUPPORTED row is therefore an anchor that cannot be inspected from here, presented at maximum confidence. This directly contradicts the USER-CONFIRMED canvas, which lists under "Ausdruecklich ungeprueft (duerfen nicht als Praemisse weitergereicht werden)": "Dass FuFirE /v1/calculate/western alle fuer den Transportvertrag noetigen Felder liefert". The PRD nonetheless uses exactly that as a premise for DATA-002 and AC-003. Konfabulations-class: ungeprueft, used as premise — while stronger LOCAL evidence (captured live fixtures) sits unused in the repo.

- **evidence** — refuted: `False`. Every anchor checks out. prd_report.md:335 quotes EVD-004 verbatim at 5/5 citing "FuFirE routers/western.py"; no FuFirE source exists here (only 6 local tooling .py files). Canvas:233-237 forbids exactly that premise. DATA-002/AC-003 presuppose it. And scripts/fufire-dump-fixtures.mts captured a REAL response into src/__fixtures__/fufire/western.json containing houses, angles, bodies, precision, provenance — stronger local evidence, uncited.
  - *narrowed:* EVD-004 (prd_report.md:335) rates an uninspectable out-of-repo anchor at confidence 5/5; half its evidence cannot be verified here. It is formally linked to API-003 (not directly to DATA-002/AC-003, which inherit the premise downstream). The premise it supports is one the USER-CONFIRMED canvas explicitly bars from being passed on. Fix: re-anchor EVD-004 to the real captured fixture src/__fixtures__/fufire/western.json (houses/angles/bodies/precision/provenance present), drop the out-of-repo .py citation or mark it unverifiable, and lower confidence accordingly.
- **consequence** — refuted: `False`. Not refuted. Acting changes TASK-002 types (model precision.provisional_fields) and adds a new TASK-009 fixture-backed test — both new files, in scope under D5. The uncited local fixture hides a live trap: FuFirE returns populated Ascendant/houses when birth_time_known=false, and no code reads provisional_fields. AC-004 is therefore an unguarded suppression duty, risking exact-looking angles in unknown mode.
  - *narrowed:* EVD-004 has zero field-level support (postWestern returns Promise<any>), and the uncited local capture src/__fixtures__/fufire/unknown-time/western.json shows FuFirE returns fully populated houses/angles with precision.provisional_fields=["ascendant","houses","mc"] when birth_time_known=false. No code reads provisional_fields (only a comment in tests/e2e/explanation-layer.spec.ts:16). So AC-004's "houses/angles unavailable" is an unguarded BFF suppression duty, not an upstream guarantee. Fixtures are used by legacy realshapes tests, just not by the new transport.
- **coverage** — refuted: `False`. No mechanism closes it. scripts/validate_pack.py:1-36 only checks file existence and literal substrings — it never resolves evidence_pointer/quote_or_pointer to real paths, nor audits confidence. The only CI (.github/workflows/deploy-cloudrun.yml) just builds/deploys. SRC-REPO-FF-001's location "FuFirE-main (7).zip" (prd_report.json:79) is absent from disk. The canvas note (western-synastry.canvas.md:236) is exactly the prose mitigation this lens excludes. Live fixture src/__fixtures__/fufire/western.json remains uncited by EVD-004.
  - *narrowed:* Drop AC-003: its source_ids are SRC-USER-002 only (prd_report.json:1419-1426), so it does not inherit the FuFirE premise. The defensible form: EVD-004 (prd_report.json:2554) claims repository_evidence at 5/5 while one of its two sources, SRC-REPO-FF-001, points to "FuFirE-main (7).zip" which is not in this repo; that unearned confidence propagates to API-003 (prd_report.json:867-877) and DATA-002 (prd_report.json:797-807), both of which list SRC-REPO-FF-001. Meanwhile src/__fixtures__/fufire/western.json — a verbatim live capture already asserted by src/utils/fufireNormalizer.realshapes.test.ts:249-254 (bodies, angles.Ascendant, 12 house cusps, precision.provisional_fields) — is stronger, locally inspectable evidence that EVD-004 does not cite.

---

## F-03 — [unsupported-claims] (refutes 0/3)

NFR-005 / AC-009 / INFO-011 require that generated DST adjustments be "surfaced in transport warnings", and the plan's transport carries `timeResolution.adjusted`. The captured live /v1/calculate/western response has NO field reporting whether an adjustment occurred — its keys are jd_ut, house_system, bodies, houses, angles, aspects, house_quality, quality_flags, provenance, precision. `adjusted` can therefore only ever be a BFF self-report of what it *requested*, never evidence of what the engine *did*. Worse, the only DST evidence in the entire repo is a 422 from `/chart` (src/__fixtures__/fufire/chart-422-dst.json, `"path": "/chart"`) — a different contract that src/utils/fufireClient.ts:17-19 explicitly warns must never be conflated with /v1/calculate/* ("never send this chart shape to them (live 422)") — and that error's own hint names a THIRD parameter (`dst_policy='earlier' or 'later'`) that does not exist in WesternRequestPayload. Meanwhile `shift_forward` has never been sent anywhere in this codebase: buildWesternPayload hardcodes `nonexistentTime: "error"`. Konfabulations-class: ungeprueft (acceptance of `shift_forward` on /v1/calculate/western; existence of any adjustment warning), used as premise for AC-009 and TEST-009.

- **evidence** — refuted: `False`. Every anchor checks out verbatim: prd_report.md:240/152/113, plan lines 427-430 (`adjusted`), western.json's ten keys with no adjustment field (a genuine live capture per fufire-dump-fixtures.mts:71), chart-422-dst.json's `"path": "/chart"` and `dst_policy` hint (absent from WesternRequestPayload), fufireClient.ts:17-19's conflation warning, and fufirePayloadMappers.ts:148 hardcoding `nonexistentTime: "error"`. `shift_forward` exists only as a type literal, never sent. Both premises are unverified.
  - *narrowed:* Slightly narrow one clause: the fixture was captured with `nonexistentTime:"error"` on a non-gap time, so its silence is also consistent with an engine that emits an adjustment field only when it actually adjusts. The defensible claim is that no repo evidence shows the engine reporting adjustments, and `/v1/calculate/western`'s acceptance of `shift_forward` is wholly untested — leaving AC-009/TEST-009 on unverified premises.
- **consequence** — refuted: `False`. Verified: western.json has no adjustment field, buildWesternPayload hardcodes nonexistentTime:"error", and the only DST fixture is /chart-shaped (explicitly forbidden for /v1 by fufireClient.ts:17-19). This changes work inside 001-013: TASK-008 step 5 ("preserve upstream DST warnings") is unimplementable and must be rewritten; step 4 needs a new-file payload builder under D5; AC-006/AC-009 must be narrowed; TEST-009 needs an ungeprueft marker.
  - *narrowed:* Narrower defensible version: (1) TASK-008 step 5 must drop "upstream DST warnings" — /v1/calculate/western returns none; `timeResolution.adjusted` must be documented as a BFF-local self-report derived from BFF-side tz-gap detection of the requested policy, never engine evidence. (2) TASK-008 step 4 cannot reuse buildWesternPayload (hardcoded "error", existing file, out of scope under D5) — a new-file western sample builder is required to emit shift_forward. (3) AC-006/AC-009/NFR-005 wording must be narrowed from "Korrektur als Warning ausgewiesen" to "requested policy + BFF-detected adjustment flag transported". (4) shift_forward acceptance by /v1/calculate/western remains ungeprueft (no staging creds, D2): TEST-009 may only assert the outgoing payload shape and the transport field, must not reuse chart-422-dst.json as western evidence, and the assumption needs an explicit Reality-Ledger entry so no acceptance claim reads as engine-verified.
- **coverage** — refuted: `False`. Verified, and nothing covers it. All mappers hardcode nonexistentTime:"error" (fufirePayloadMappers.ts:148/163/175/186/197); "shift_forward" exists only in type unions (:40/53/63/72/82) — never in a request, test or fixture. Only DST artifact is chart-422-dst.json ("path":"/chart"), which fufireClient.ts:17-19 warns not to conflate. western.json has no adjustment field. fufire-live-smoke.mts is manual, sends "error", asserts only bodies/houses. deploy-cloudrun.yml runs no tests.
  - *narrowed:* Both premises behind AC-009/TEST-009 are unverified against /v1/calculate/western: (a) that the endpoint accepts nonexistentTime="shift_forward" — never sent anywhere, only asserted by a hand-written type union commented as "mirror the engine's OpenAPI request models"; (b) that any engine field reports an adjustment occurred — the captured response's metadata channels (quality_flags, precision.provisional_fields) contain no such field. Slightly weaker than the original: it is not proven the engine has NO adjustment channel (the capture is a non-DST time), and the /chart 422 body does emit the literal string nonexistentTime='shift_forward', weak evidence the token is understood on at least one contract — though that same hint names a third parameter (dst_policy) absent from WesternRequestPayload, which deepens rather than resolves the uncertainty. Under D2 (no credentials, no live smoke), TEST-009 can only assert the BFF's own echo and would pass green while proving nothing about engine behavior. Mark AC-009/TEST-009 as ungeprüft premise; do not count TEST-009 toward AC-009 until a live boundary check exists.

---

## F-04 — [unsupported-claims] (refutes 0/3)

Two problems inside one SUPPORTED row. (a) EVD-003 cites "nb_profiles migration with RLS" at confidence 5/5 as evidence for the FR-001 owner boundary — but the BFF connects with SUPABASE_SERVICE_ROLE_KEY (src/server/supabase.ts:10,16) and the policies are granted `to authenticated` (supabase/migrations/20260612_p3_foundation.sql:31,33). The service role bypasses RLS entirely; RLS contributes ZERO defense-in-depth on this path. The ONLY control is the explicit `.eq("user_id", …)` in each handler. Citing RLS as a supporting layer for a p0 privacy control (RISK-006 — "could expose or process another user's birth data") tells an implementer there is a backstop that does not exist: omit `.eq("user_id")` in the new route and nothing catches it. (b) The cited range src/server/app.ts:820-865 contains no owner-filtered read-BY-ID. The list read (820-829) filters by user_id only; the only `id + user_id` pair in the file is on DELETE (862-863). FR-001 / TECH-INV-03 need a SELECT-by-id-with-user_id that has no precedent in this codebase — so "can be reused" is an INFERENCE from a delete-shaped query, classified as SOURCE_EVIDENCE.

- **evidence** — refuted: `False`. All anchors verified verbatim. supabase.ts:16 uses SERVICE_ROLE_KEY; migration:31,33 grant `to authenticated`, and the migration's own comment states "Service-Role umgeht RLS (BFF)" — so RLS demonstrably contributes zero defense on this path, yet EVD-003 (prd_report.md:334) cites it at 5/5 for p0 RISK-006 with no such limitation noted. For (b), grep shows the only `eq("id")` uses in app.ts are lines 862 and 906, both DELETEs; 820-829 filters by user_id only. No owner-filtered SELECT-by-id exists, so reuse is inference. D5 (app.ts out of scope) sharpens this: the new file has no precedent to copy.
  - *narrowed:* Stands as written. Only scope note: RLS would defend a path using an anon/authenticated key — it is zero specifically on the service-role BFF path, which the finding already states correctly. RISK-006's mitigation text does name "id + user_id lookup" properly; the defect is confined to EVD-003's evidence citation, which the finding targets precisely.
- **consequence** — refuted: `False`. Both claims verified. Service-role bypasses RLS (migration's own comment says so); policies are `to authenticated`. grep proves the only id+user_id pairs in app.ts (862, 906) are DELETEs — no SELECT-by-id precedent. Acting changes TASK-008 (must use maybeSingle; copying .single() at app.ts:849 returns 502 not the required 404) and TASK-007's AC (adopt the explicit eq-filter assertion already used at app.reflections.test.ts:155).
  - *narrowed:* EVD-003 is doubly defective and both defects are actionable inside TASK-001..013. (a) The "nb_profiles migration with RLS" citation must be struck as supporting evidence for FR-001: the BFF connects with SUPABASE_SERVICE_ROLE_KEY (src/server/supabase.ts:16) and policies are granted `to authenticated` (20260612_p3_foundation.sql:31,33), so RLS is zero defense on this path — the explicit .eq("user_id") is the sole control for a p0 privacy boundary (RISK-006). (b) The range src/server/app.ts:820-865 contains no owner-filtered read-by-id; both .eq("id") uses (862, 906) are DELETEs, so "can be reused" is SOURCE_EVIDENCE inference, not repository_evidence at 5/5. Required actions: (1) TASK-008 — the by-id load must use .maybeSingle()/select-array and map empty to 404; copying the .single() pattern at app.ts:849, whose error branch is sendError(db_error, 502), would return 502 for a foreign profile and violate TASK-008's own AC; (2) TASK-007 — raise the AC beyond a 404-shape test to assert the filter explicitly, reusing the existing precedent at src/server/app.reflections.test.ts:155-157, which carries the comment "Service-Role bypasst RLS → Query MUSS ... eq('user_id', ...) tragen"; (3) correct EVD-003's citation, line range and confidence in docs/prd/prd_report.md:334 and prd_report.json. Narrowing: the finding slightly overstates "no test" — TASK-007 step 2 does plan a foreign-profile 404 test, and the in-memory Supabase double in app.profiles.test.ts does honor filters, so such a test would catch a wholly omitted filter. The real gap is that the AC does not require the explicit eq-filter assertion, leaving the guarantee dependent on test-double fidelity rather than on a direct assertion.
- **coverage** — refuted: `False`. Both claims verified. (a) src/server/supabase.ts:16 uses SUPABASE_SERVICE_ROLE_KEY; policies are `to authenticated` (migration :31,33,36,38) — the migration's own header (:3) admits "Service-Role umgeht RLS". (b) app.ts:823-825 list = user_id only; the sole id+user_id pairs are DELETEs (:862-863, :906-907). No SELECT-by-id-with-user_id exists. Lens 3: no coverage — `lint` is only tsc, no ESLint, CI runs deploy only (no test job), no owner-scoping helper, no route-inventory guard. Existing per-route tests cannot see a new route.

---

## F-05 — [unsupported-claims] (refutes 0/3)

"Plan and PRD satisfy their structural contracts | Actual validator outputs | SOURCE_EVIDENCE | SUPPORTED" does not survive contact with the repo. Three of the five cited validators (validate_prd_output.py, prd-output.schema.json, validate_sarif.py) live under /home/oai/skills/... and are absent here; every run path is /mnt/data/western-synastry-v2 — a different machine. Item 6, the "Cross-document validator" that carries the strongest claims (unique IDs, resolving trace references, P0 traceability, stale-term checks), lists checks but NO command, and nothing in scripts/ implements it — it is an unreproducible claimed validation. The one locally runnable validator, scripts/validate_pack.py, re-runs PASS here but is a file-existence + substring checker written by the same author chain alongside the artifacts it validates: it asserts things like `'real-boundary' in plan` and `'final_decision' in prd_json`. It cannot fail on any content error. Konfabulations-class: ungeprueft (validator provenance) presented as SOURCE_EVIDENCE.

- **evidence** — refuted: `False`. All anchors verified. /home/oai/skills and /mnt/data do not exist here; report lines 6/14/20/26 cite them. Item 6 has Checks+Result but no Command, and scripts/ (only validate_pack.py + .mts smokes) implements none of it. validate_pack.py is exactly file-existence + literal substring checks, and shipped in the same commit b0fc5e9 as the artifacts it validates. The row's only caveat covers runtime, not validator provenance.
  - *narrowed:* Same finding with two tightenings: it is four of five (validate_plan.py is also under /home/oai/skills), and validate_pack.py can fail — on a missing file or missing literal token — but cannot fail on any semantic content error. Also: unreproducible here means unverifiable, not proven-never-run; item 6 is the stronger case, since with no command it is a claimed result rather than a validator output.
- **consequence** — refuted: `False`. Verified: /home/oai and /mnt/data absent; report item 6 has "Checks:" but no "Command:" (items 1-5 have one) and nothing in scripts/ implements it; validate_pack.py is existence+substring only; no npm script or CI invokes any doc validator. Actionable under NEW-FILES-ONLY: add scripts/validate_crossdoc.py plus a new vitest that runs it (auto-collected, no package.json edit), and downgrade the SOURCE_EVIDENCE/SUPPORTED row.
  - *narrowed:* Narrow the "cannot fail on any content error" claim: validate_pack.py does fail on a deleted required file, a missing DEC-20260719-0N marker, or a removed plan term. Defensible core: 4 of 6 validators are unreproducible in this repo, item 6 has no command and no implementation, no automation invokes any of them, and the one that runs checks existence and substrings only — so classifying "Plan and PRD satisfy their structural contracts" as SOURCE_EVIDENCE/SUPPORTED overstates what is reproducible here. Drop "same author chain" as provenance suspicion rather than proven defect.
- **coverage** — refuted: `False`. Verified, not covered. Mutation test: plan replaced with garbage containing the six substrings still yields PACK VALIDATION: PASS (exit 0) — validate_pack.py provably cannot fail on content. /home/oai/skills and /mnt/data absent. Repo-wide grep finds no cross-document validator implementation. Only workflow (deploy-cloudrun.yml) runs gcloud deploy; no package.json script invokes any validator. Sole mitigations are prose (EVIDENCE_DIALECTIC_VALIDATION.md:31, validation-report.txt:55) disclaiming runtime, not validator provenance.
  - *narrowed:* Two narrowings only: validate_pack.py:29 does json.loads(), so it would catch malformed JSON (it is file-existence + substring + JSON-parse, not pure substring); and "written by the same author chain" is provenance inference rather than demonstrated fact. The substantive claims — three cited validators absent on this machine, item 6 unimplemented and uncommanded, item 5 unable to fail on content error, and no test/CI/lint mechanism anywhere making this visible — all hold as stated.

---

## F-06 — [unsupported-claims] (refutes 1/3)

docs/prd/code_findings.sarif.json anchors all seven results to `docs/prd/prd_report.json` with no region — not a single finding points at code. Yet F-001, F-002 and F-007 concern specific, verifiable source lines that exist and that I confirmed by hand (src/utils/synastry.ts:47 compareProfiles; src/utils/birthInputValidation.ts:97 `birthTime = "12:00"`; src/server/app.ts:455 the compute-limiter array). The PRD's Artifact Gate reports PASS with "Markdown, JSON and SARIF artifacts are emitted and validated", and the validation report records "PASS: SARIF structure" — both true and both empty: a code-findings artifact containing zero code anchors is the precise failure mode SARIF exists to prevent. A downstream consumer wiring this SARIF into a code scanner gets seven findings all pointing at the PRD that generated them.

- **evidence** — refuted: `False`. Every anchor verified verbatim. All 7 SARIF results point at docs/prd/prd_report.json with no region (checked programmatically). compareProfiles is synastry.ts:47; birthTime = "12:00" is birthInputValidation.ts:97; the compute-limiter array is app.ts:455. The Artifact Gate quote matches prd_report.md:438; "PASS: SARIF structure" matches validation-report.txt:28. The inference holds: a code-findings SARIF with zero code anchors is self-referential and useless to a scanner.
  - *narrowed:* Sharpening only, not a narrowing: the SARIF is spec-conformant, since `region` is optional in SARIF 2.1.0. The defect is semantic, not schema-level — which is precisely why "PASS: SARIF structure" and the Artifact Gate PASS are true yet vacuous, and the finding already targets that gate language rather than claiming invalidity. Scope is also correctly limited to F-001/F-002/F-007; F-004 and F-005 are process findings with no single code line, so the claim does not overreach.
- **consequence** — refuted: `True`. Verified true: all seven results anchor to docs/prd/prd_report.json with no region. But no TASK-001..013 emits, validates, or consumes the SARIF; nothing in the repo wires it to a scanner; and "Artifact Gate PASS" appears only in prd_report.md, never in the plan, decisions, or confirmed canvas. The file is git-tracked and pre-existing, so D5's new-files-only scope forbids fixing it here. No file, test, or 001..013 acceptance claim changes.
  - *narrowed:* The defect is real and should not be dismissed as "documented" — it is simply out of this run's band. Narrower defensible version: code_findings.sarif.json emits seven code-domain findings with zero code anchors, so its Artifact Gate PASS is structurally hollow. Log it as an explicit limitation in TASK-015's evidence ledger and as a line item for the separate, explicitly-requested PRD-artifact release that also carries F-007's src/server/app.ts limiter change.
- **coverage** — refuted: `False`. No mechanism covers this. scripts/validate_pack.py:8,19 only checks the SARIF file EXISTS — it never parses it (contrast line 29, which json.loads the PRD JSON). The "PASS: SARIF structure" at validation/validation-report.txt:28 invokes /home/oai/skills/.../validate_sarif.py, a path that does not exist here; it is a transcribed prose claim, not a re-runnable check. No test greps sarif/code_findings; .github/workflows holds only deploy-cloudrun.yml. Facts verified: all 7 results anchor to prd_report.json with no region, and synastry.ts:47, birthInputValidation.ts:97, app.ts:455 exist as cited. Fix (editing the SARIF) is D5-permitted.
  - *narrowed:* Narrowest defensible version: the five findings whose verification_status is "source_inspected*" (F-001, F-002, F-003, F-006, F-007) cite specific, hand-confirmed source lines yet anchor to docs/prd/prd_report.json with no region, so the SARIF carries zero code anchors. F-004 and F-005 are process/decision findings with legitimately no code location. The Artifact Gate PASS and "PASS: SARIF structure" remain unearned either way, since neither is backed by a check that inspects result locations.

---

## F-07 — [unsupported-claims] (refutes 0/3)

INFO-002 and INFO-003 carry status `verified` and the user-confirmed canvas §1 asserts as *belegt* that under the 12:00 substitution "Zeitabhaengige Signale — Mond, Haeuser, Achsen — werden damit als exakt behandelt". The repository contradicts the houses/axes half. src/utils/fufireNormalizer.ts:237-238 marks ascendant and houses provisional whenever `!timeKnown`; :329 returns `ascendant = null`; :350 returns `houses = []`; and src/__fixtures__/fufire/unknown-time/README.md documents this as the deliberately fixture-tested F-01 short-circuit ("Test fixture deliberately has angles.Ascendant non-null to prove the short-circuit works"). Only the Moon survives unflagged — moonSign is read at :326 with no provisional check at all. So the stated problem is materially narrower than claimed, and it is mis-sized in both directions: AC-004's houses/angles rule is largely already enforced upstream and duplicates existing behaviour, while the ACTUAL unmitigated gap — fast-moving bodies, above all the Moon — is never named as the thing the new product must fix.

- **evidence** — refuted: `False`. Every anchor checks out verbatim: :237-238 gate on !timeKnown, :329/:339 null the ascendant, :350 empties houses, the fixture README contains the quoted F-01 line, canvas §1:35-37 matches, and INFO-002/003 are both `verified`. moonSign at :326 has no provisional check anywhere in src, and :721 emits it unconditionally while :722 excludes the ascendant. The mis-sizing inference follows directly.
  - *narrowed:* The houses/angles half of AC-004 duplicates existing normalizer behaviour only for the unknown-time branch (`!timeKnown`); the approximate/day-part case is genuinely new, since `timeKnown` is a boolean and approximate profiles still return houses and an ascendant. The unmitigated gap — the Moon and other fast bodies, read at :326 with no provisional gate and emitted unconditionally at :721 — is real and unnamed in INFO-003.
- **consequence** — refuted: `False`. Half is wrong: the new flow never touches fufireNormalizer (TASK-008 calls postWestern directly; zero grep hits in plan/PRD/canvas), so TASK-010's houses/angles exclusion in sampleStability.ts is a new guard, not duplicated behaviour. But the Moon half holds and is actionable: grep finds no Moon reference in any task or test, and TASK-010's test list never exercises a fast body degrading across a band.
  - *narrowed:* Drop the "AC-004 duplicates upstream behaviour" claim — fufireNormalizer is a separate legacy path, and its unguarded moonSign at :326 is out of scope under D5. Keep the narrower point: the canvas §1 "Mond, Häuser, Achsen ... als exakt behandelt" claim has no executable proof for the Moon in the new flow. Add to TASK-010 a test in src/utils/relationship/sampleStability.test.ts asserting a Moon-based aspect present at the 12:00 midpoint sample but absent at 00:00/23:59 classifies provisional or unavailable, never stable.
- **coverage** — refuted: `False`. Half verified: fufireNormalizer.ts:237-238 already nulls ascendant/houses on !timeKnown, tested at fufireNormalizer.test.ts:193/198 without engine flags — so the canvas houses/axes claim is stale. But the finding's other half is wrong: prd_report.md:346 (F-002) and :337 (EVD-006) explicitly name Moon outputs appearing exact. And AC-004 is not duplicative — no approximate/timeBand exists in src.
  - *narrowed:* Canvas §1 and INFO-003 overstate the unknown-time problem on one axis only: houses/angles are already nulled and tested (fufireNormalizer.ts:237-238; fufireNormalizer.test.ts:193,198), so that portion of AC-004's unknown-time rule restates shipped behaviour. AC-004's approximate/time-band mode and its pair-level "both must be exact" rule remain genuinely new — grep finds no approximate/timeBand in src, only boolean timeKnown. The real uncovered gap is the Moon: fufireNormalizer.ts:326 reads moonSign with no provisional guard and no unknown-time test asserts on it. That gap is named in the PRD (prd_report.md:346) but has no code or test mechanism.

---

## F-08 — [unsupported-claims] (refutes 0/3)

"New route must join the compute rate limiter | INFERENCE | SUPPORTED | Requires implementation and route test" is mechanically right (src/server/app.ts:455 is a path-prefix array) but carries two things it does not say. (a) The limiter's VALUE is unchanged at `RATE_LIMIT_COMPUTE_MAX || 20`, calibrated for /api/azodiac routes making one or two upstream calls; the new route fans out to six (NFR-003, SUS-001, and F-007's own words "one request can fan out to six paid upstream calls"). Merely joining the group therefore raises worst-case upstream spend per IP per window by roughly 3-6x — the mitigation the PRD credits for F-007 does not restore the previous cost bound. (b) Joining requires editing src/server/app.ts:455, which the user-confirmed canvas places out of scope, so AC-010 / TEST-010 / SEC-006 / API-007 are unreachable in this run; and under NODE_ENV=test the limiter self-skips unless RATE_LIMIT_FORCE=true (app.ts:52-53), which the PRD's own validation command list never sets — the existing app.ratelimit.test.ts only passes because it sets that env internally, and the canvas puts that file out of scope too.

- **evidence** — refuted: `False`. All anchors verified. app.ts:455 is the prefix array with COMPUTE_MAX default 20, unchanged; azodiac routes are 1-2 upstream calls while PRD F-007/NFR-003/SUS-001 confirm six for the new route, so joining the group does not restore the cost bound. Canvas:272/329 puts app.ts out of scope, making AC-010/TEST-010/SEC-006/API-007 unreachable. app.ts:52-53 self-skips under test; PRD validation commands never set RATE_LIMIT_FORCE; ratelimit.test.ts:19 sets it internally; canvas:286 excludes that file.
  - *narrowed:* Sound as written. Two refinements: the "3-6x" multiplier is an estimate whose baseline varies (the profile route itself makes more than one upstream call), and the canvas does route the relationship rate-limit proof to the in-scope NEW file src/server/app.relationship.test.ts — but that file still cannot prove 429 without the out-of-scope app.ts:455 edit, and would pass vacuously if it omits RATE_LIMIT_FORCE=true. Neither weakens the conclusion.
- **consequence** — refuted: `False`. Verified: app.ts:455 caps at 20, calibrated for 1-2 upstream calls; the new route fans out to six, so joining unchanged raises worst-case spend 3-6x. Plan TASK-008 step 1 (line 714) mandates an out-of-scope app.ts edit, and TASK-007 step 5 mandates a 429 test whose named file is also out of scope — and under NODE_ENV=test the limiter self-skips (app.ts:52-53), so any new test omitting RATE_LIMIT_FORCE passes vacuously. All inside TASK-001..013.
  - *narrowed:* Both sub-claims hold, but the conclusion "AC-010/SEC-006/API-007 are unreachable" is too fatalistic: under D5 (new files only), the new relationship router can mount its own dedicated limiter — a stricter, separately-tunable max reflecting six-call fan-out, not the borrowed 20 — inside the new route module, and a NEW test file that sets RATE_LIMIT_FORCE=true can prove 429 before fan-out against that router via supertest. Actions for this run: (1) rewrite TASK-008 step 1 to build a route-local limiter instead of editing app.ts:455; (2) put TASK-007's 429 test in a new file, not src/server/app.ratelimit.test.ts, and assert zero upstream calls after 429; (3) that new test must force-enable the limiter or it is green-but-meaningless; (4) the run's acceptance ledger must state that production-path rate-limit coverage still requires the separately-released app.ts wiring, so SEC-006/API-007 are satisfied only at module level, not at the deployed boundary.
- **coverage** — refuted: `False`. Not covered: the repo has no test/lint CI at all — .github/workflows/ holds only deploy-cloudrun.yml — so nothing flags an unjoined route. But claim (a) is false: /api/azodiac/synastry (app.ts:687) already runs 2× resolveProfile, each up to 5 upstream calls (profileService.ts:46-77) = worst case 10, exceeding the new route's 6; app.ts:455 is one shared store, so joining adds no budget. No 3-6x increase.
  - *narrowed:* Drop part (a): the compute limiter is a single shared-store instance (src/server/app.ts:455) and the existing worst case is already ~10 upstream calls per request (app.ts:687 → 2× resolveProfile, each 1 /chart + up to 4 /v1/calculate/*, profileService.ts:46-77), so a 6-call route joining the group does not raise the per-IP cost bound. What survives: the only join point is app.ts:455, which D5 puts out of scope, so AC-010 / TEST-010 / SEC-006 / API-007 are unreachable this run, and no mechanism records that — the sole CI workflow is a Cloud Run deploy (.github/workflows/deploy-cloudrun.yml) with no test/lint/tsc step, leaving canvas prose (:205, :278-283) as the only trace. Secondary, lower severity: vitest.config.ts:10 sets DISABLE_RATE_LIMIT=true suite-wide, so a relationship rate-limit test must set RATE_LIMIT_FORCE itself (pattern at src/server/app.ratelimit.test.ts:19) — but omitting it fails red, not silently green, so this is an author-time trap rather than a false-green risk.

---

## F-09 — [untested-failure-modes] (refutes 1/3)

The project's headline failure mode — "a mocked upstream carries the real-boundary claim" (DEC-07, DEC-08, CTR-001, canvas R9) — is guaranteed live by the existing Playwright config, and its named test (TEST-006 / TASK-014) runs against the mock by construction. `npm run e2e` is `playwright test` (package.json:13), and playwright.config.ts:20-46 unconditionally starts `node tests/e2e/mock-fufire.mjs` as a webServer and boots the app with `FUFIRE_API_URL=http://localhost:8799` and `ENABLE_DEMO_PROFILES: "true"`. The plan's real-boundary command (plan:883, PRD:411) inherits that webServer verbatim. TASK-014's acceptance "No mocked BFF, route interception, fixture response or demo profile is active" (plan:879) is prose with no mechanism behind it, `RELATIONSHIP_REAL_BOUNDARY=1` is read by nothing, and playwright.config.ts appears neither in the canvas allowed-change-scope list (canvas:250-262) nor in the known scope-stop table (canvas:271-276). Neither existing review mentions the file.

- **evidence** — refuted: `False`. Every anchor verified verbatim. package.json:13 is `playwright test`; playwright.config.ts:20-44 unconditionally starts the FuFirE mock and boots the app at it with ENABLE_DEMO_PROFILES=true, reuseExistingServer:false. The plan's real-boundary command inherits that webServer. RELATIONSHIP_REAL_BOUNDARY has 9 hits, all documentation, none in code. playwright.config.ts is absent from canvas:250-262, canvas:271-276, and both reviews. The inference holds.
  - *narrowed:* Two precision fixes, neither weakening the claim: the webServer block ends at line 44 (file is 45 lines), not 46; and under D2 this run stops at TASK-013, so the defect is latent — a plan/config contradiction plus an unanticipated scope stop blocking any future TASK-014, not a fault in code shipped by this run.
- **consequence** — refuted: `True`. Factually correct but out of window. Every TASK-001..013 validation command (plan:523-860) is vitest; none runs `npm run e2e`, and vitest.config.ts excludes `tests/e2e/**`, so the mock webServer is unreachable this run. `RELATIONSHIP_REAL_BOUNDARY` and the spec that would read it are TASK-014 artifacts. D5 also bars editing existing playwright.config.ts. Only action is a docs note — no file, test, or acceptance claim inside 001..013 changes.
  - *narrowed:* Defensible as a TASK-014 blocker, not a TASK-001..013 finding: the named real-boundary test cannot honor its acceptance criterion because playwright.config.ts unconditionally injects the mock upstream and demo profiles, `RELATIONSHIP_REAL_BOUNDARY` is dead prose, and playwright.config.ts sits in neither the canvas allowed-scope list nor the scope-stop table — an unpredicted hard stop awaiting TASK-014. Carry into the handoff as an owned TASK-014 precondition plus a canvas scope-stop-table entry; do not act on it inside this run.
- **coverage** — refuted: `False`. No mechanism exists. Only CI (deploy-cloudrun.yml) never runs e2e; validate_pack.py:26 greps plan prose; the evidence schema only enum-checks the self-asserted label; mapsService.ts:98-102 inverts protection (NODE_ENV="test" forces demo mocks on). Worse than stated: Playwright's webServer.env overrides process.env, so exported staging credentials still lose to FUFIRE_API_URL=localhost:8799, and reuseExistingServer:false blocks substitution. Fix is scope-blocked (canvas:250-276).
  - *narrowed:* Narrower: TASK-014 is out of scope this run (D2), so the defect is latent rather than currently realized. The defensible core is that the plan's documented real-boundary command (plan:883) provably boots the mock and cannot be redirected from the shell, RELATIONSHIP_REAL_BOUNDARY is inert, and playwright.config.ts sits outside the allowed-change scope — so this requires a separate explicitly-requested release before TASK-014 can ever produce honest real-boundary-smoke evidence.

---

## F-10 — [untested-failure-modes] (refutes 0/3)

Every assertion that the MVP produces real patterns carries an "or an honest Missing-State" disjunction, so a flow that computes nothing satisfies the acceptance criteria, the analyzer tests and the real-boundary test simultaneously. Plan:29 ('mindestens drei echte, berechnete Muster oder ehrliche Missing-States'), AC-007 (plan:241), TASK-014 step 4 (plan:873) and TASK-011's acceptance 'Output contains zero to three evidence-linked patterns' (plan:799) all accept the empty branch. TASK-011's test steps (plan:792-796) only test ordering (stable outranks provisional, unavailable never headlines) and absence of a score — none requires a non-empty result. The silent-empty path is real: `computeInterAspects` returns `[]` for empty input (src/utils/interAspects.ts:135-137) and `bodyPositionsFromViewModel` silently skips bodies whose sign name is not in the German `SIGN_INDEX_DE` map (src/utils/interAspects.ts:102-104).

- **evidence** — refuted: `False`. All eight anchors verified verbatim at the cited lines; only interAspects.ts:135-137 is off by ~2 (return [] sits at 133-135), which is not a misquote of substance. The disjunction is genuinely present in all four assertions, TASK-011 steps 792-796 require no non-empty result, and the silent-empty path is real: unknown sign names are skipped at 103-104, empty input returns [] at 133-135. Inference holds.
  - *narrowed:* Tighten the cite to interAspects.ts:133-135 (not 135-137). Note that interAspects.test.ts does assert non-empty results, but only via makeVm fixtures that already use German sign names — so the math and adapter happy path are pinned while the assembled flow's total-computation-failure mode (upstream sign strings outside SIGN_INDEX_DE) remains undetectable by every cited criterion.
- **consequence** — refuted: `False`. Actionable inside scope. relationshipAnalyzer.ts and its test do not exist yet — TASK-011 is written this run. Its steps (plan:792-796) test only ordering and score-absence, and acceptance "zero to three patterns" admits the empty branch, so a totally silent analyzer would pass. The silent-empty path is real (interAspects.ts:102-104 skips unknown signs; :135-137 returns []). Fix costs one positive-path test plus one sign-map guard test.
  - *narrowed:* Scoped to TASK-011 (and its TASK-010 input): the analyzer's test file must include a positive-path case where a realistic two-profile viewmodel pair yields at least one real, evidence-linked pattern, plus a guard test that bodyPositionsFromViewModel returns non-empty for canonical German sign names — so a SIGN_INDEX_DE mismatch fails loudly instead of degrading to Missing. The AC-007/TASK-014 half of the finding is out of scope for this run.
- **coverage** — refuted: `False`. No mechanism closes this. The only production assertion, src/server/app.test.ts:481 `expect(Array.isArray(res.body.interAspects)).toBe(true)`, passes on []. interAspects.test.ts:276 hand-builds German signs, so it cannot detect a real producer emitting none. The realshapes test asserts planets.length>=10 and top-level sunSign, never per-planet .sign — and fufireNormalizer.ts:325 sources sunSign upstream, bypassing SIGN_INDEX_DE. ProfileViewModel types sign as string; lint is bare tsc; no coverage thresholds; no test CI.
  - *narrowed:* Narrower defensible core: no test anywhere asserts that a realistic profile-pair input yields at least one body position or inter-aspect, so an all-empty pipeline satisfies every acceptance criterion, the analyzer tests and the real-boundary test at once. Partial nuance: TASK-011 step-1 ordering tests inject signals directly, so a constant `return []` analyzer stub would fail them — the uncovered failure mode is upstream emptiness (bodyPositionsFromViewModel returning [] on real data), not a stubbed analyzer. Fix is available within D5's new-files-only scope: a new test asserting non-empty positions/aspects from a real-shaped normalized view-model pair.

---

## F-11 — [untested-failure-modes] (refutes 0/3)

RISK-001 / canvas R1 ("consent copy is read as verified consent") is mitigated in prose by wording, but the only tests are the 422 gate. TEST-001 (PRD:247) and AC-001 test status code and zero upstream calls — both stay green no matter what the copy says. TASK-006 names the acceptance "Copy does not misrepresent verification" (plan:669) and instructs "Include wording: user confirms; application does not independently verify" (plan:665), but its test steps (plan:661-662) only assert the submit button stays disabled until the checkbox is ticked. The wording control appears only in the *manual* review checklist (plan:1021). The repo already ships a forbidden-wording scanner (src/__tests__/synastryWording.test.ts:19, a regex over runtime copy and component chrome), and the new scanner is deliberately scoped to score/`compareProfiles` only (canvas:171-177).

- **evidence** — refuted: `False`. All anchors verified verbatim: PRD:247, AC-001, plan:661-662/665/669/1021, canvas:171-177. TASK-006 states acceptance "Copy does not misrepresent verification" but specifies no assertion for it; only the manual checklist covers wording, and TEST-013 is `blocked`. TASK-007 also never asserts transport `verificationStatus`. The 422 tests stay green regardless of copy. Sole defect: the regex sits at line 17-18, not 19 — nothing was quoted from :19.
  - *narrowed:* Cite the scanner as src/__tests__/synastryWording.test.ts:17-18 (declaration plus regex body) rather than :19. Otherwise the finding stands as written: RISK-001's wording mitigation has no executable gate anywhere in TASK-001..013 — at the UI copy layer or the transport `verificationStatus` field — leaving only plan:1021's manual checklist and the blocked TEST-013.
- **consequence** — refuted: `False`. Actionable inside TASK-006. Its acceptance "Copy does not misrepresent verification" (plan:669) has zero executable check — plan steps 661-662 only assert submit stays disabled, and TEST-001/AC-001 pass regardless of copy. The control lives only in a manual checklist (plan:1021). Fix is a new-file assertion in ReviewConsentStep.test.tsx (TASK-006's own new test), using the existing synastryWording.test.ts regex-scanner pattern. Fully compatible with D2/D4/D5.
  - *narrowed:* Narrower: RISK-001's *enforcement* half is tested (422 gate + disabled submit). What is untested is only the *wording* half — that the consent string asserts user confirmation and never claims independent verification. Add to src/components/relationship/ReviewConsentStep.test.tsx (new file, in scope): assert the relationship-consent-v1 copy matches attestation wording and does not match a verification-claim regex (verifiziert/geprüft/bestätigt/validated). No change to synastryWording.test.ts or the score scanner.
- **coverage** — refuted: `False`. No such mechanism exists. synastryWording.test.ts:19 PAIR_FORBIDDEN lists only reification terms — zero verification terms (verifiziert/geprüft/bestätigt) — and its component sweep is a hardcoded 3-file P7 list that D5 freezes. redesignWording.test.ts:19 is landing-scoped, same absence. relationshipNoScore.test.ts is score-only (canvas:171-177) and unwritten. No eslint config; deploy-cloudrun.yml runs no tests. TEST-001 asserts status 422 only; plan:1021 is manual.
  - *narrowed:* Precise scope: PRD SEC-001 ("never represented as externally verified", prd_report.md:182) traces only to AC-001/TEST-001 (prd_report.md:326), a status-code test — so the copy half of the requirement is untested by construction, not merely under-tested. The transport field verificationStatus (plan:410) is JSON, not user-visible copy. Remedy is in scope under D5: a new ReviewConsentStep wording test asserting attestation phrasing present and verification-claim regex absent.

---

## F-12 — [untested-failure-modes] (refutes 0/3)

RISK-002 / canvas R2 names two concrete wording failures — "Der Text darf nie 'gilt im ganzen Zeitfenster' behaupten" (canvas:155) and "der Mittelpunkt wird nicht als gemessene Geburtszeit bezeichnet" (plan:221, restated plan:497 rule 13 and plan:1024) — and neither has a test. The named cover, TEST-005 (PRD:251) / TASK-010 (plan:766-771), verifies the `ceil(totalPairs/2)` arithmetic and role preservation. That arithmetic is green regardless of what the result screen says; a UI that computes `provisional` correctly and then renders "gilt in Ihrem gesamten Zeitfenster" next to a midpoint labelled "Geburtszeit" passes TEST-005, TEST-007 (score-only scan) and every TASK-010/011/012 acceptance criterion.

- **evidence** — refuted: `False`. Every anchor checks out verbatim: canvas:155, plan:221/497/1024, PRD:251 TEST-005 ("Aspect symmetry, role preservation, center-plus-majority... no generic confidence"), PRD:252 TEST-007 (score/percentage scan), plan:766-771. Neither wording rule appears in any acceptance criterion or automated test; grep "Zeitfenster" over src/ is empty, and synastryWording.test.ts is a P7 verdict-word scanner over legacy components only. The inference holds.</parameter>
<parameter name="corrected_form">Precisely: no *automated* test asserts either rule. The nearest coverage is human or blocked — TEST-013 (human review, `blocked`), the manual checklist at plan:1024, and TEST-006 step 5 (real-boundary, excluded by D2), which only confirms a sampled status is *visible*, not that interval claims are absent or that the midpoint is not labelled a birth time.
- **consequence** — refuted: `False`. AC-004 normatively forbids labelling the midpoint a measured Geburtszeit; canvas:155 forbids whole-window claims. Traced covers TEST-002/TEST-005 assert validator and ceil() arithmetic only. The existing scanner (synastryWording.test.ts:76) hardcodes three legacy files and is out of scope under D5, so it can never see src/components/relationship/**. The only other cover is the blocked TASK-016 manual checklist. Fix is a new-file scanner plus wording assertions in TASK-011/012 acceptance criteria — fully allowed.
  - *narrowed:* AC-004's midpoint clause traces to TEST-002 as well as TEST-005; neither asserts rendered copy, so the substance holds. Scope the remedy as a new file src/__tests__/relationshipWording.test.ts scanning src/content/relationship/**, src/components/relationship/** and src/utils/relationship/** for the two forbidden claim patterns, mirroring the D4-approved relationshipNoScore.test.ts pattern, plus one explicit wording acceptance criterion added to TASK-011 and TASK-012.
- **coverage** — refuted: `False`. No mechanism covers either phrase. All four scanners lack the terms: wordingHonesty.test.ts:5,10; synastryWording.test.ts:18; redesignWording.test.ts:18,54,67. Their file scans are non-recursive (wordingHonesty.test.ts:17), landing-only (:24) or a hard-coded three-path list (synastryWording.test.ts:74), so src/components/relationship/* is unscanned. grep "Zeitfenster" over src+tests returns zero. Only cover is TEST-013 (prd:258), a blocked manual checklist. No ESLint; lint = tsc.
  - *narrowed:* Finding stands as written. Minor scoping note: the gap is live because the result component (src/components/relationship/RelationshipMvpResult.tsx, TASK-012) is a new in-scope file, even though D5 leaves the route unwired.

---

## F-13 — [untested-failure-modes] (refutes 0/3)

The #1 failure mode of the existing craftsmanship review — "Wenn der Browser Rohdaten für Person A frei einreichen … kann, wird Eigentum umgangen" (ULTRATHINK:26) — produced tests for only half of itself. The plan states "raw Person-A birth data are not accepted as an alternative path" (plan:362), but TASK-003's validation matrix (plan:583) covers invalid UUID, missing/false consent, unsupported consent version, invalid bands, exact-without-time and unknown-with-exact — not a request carrying Person-A birth fields. TASK-007's route tests (plan:686-691) cover 401, 404 on foreign/missing ID, 422, per-mode call counts, rate limit and log redaction — again no raw-data case. TEST-003/TEST-004 (PRD:249-250) inherit that list.

- **evidence** — refuted: `False`. Every anchor verified verbatim: ULTRATHINK:26, plan:362, plan:583 (six cases, none raw-data), plan:686-691 (six cases, none raw-data), PRD:249-250. The inference holds: a stated hard boundary has no enumerated test. ULTRATHINK:26's own mitigation list covers only the foreign-profile half, confirming the omission. Documentation of intent at plan:586/591 is design, not coverage, and does not refute the test gap.
  - *narrowed:* No enumerated test case in TASK-003, TASK-007, TEST-003 or TEST-004 asserts behavior for a request carrying Person-A raw birth fields, leaving the plan:362 boundary unverified and reject-vs-silently-strip undistinguished. The intent is stated (plan:586 strict validator, plan:591 acceptance criterion), so this is a coverage gap, not a design gap.
- **consequence** — refuted: `False`. TASK-003's acceptance criteria state "Person A is represented by profile ID, not a second raw birth-data path" — but its Step-1 test list omits that case entirely. Aggravating: no zod in package.json; birthInputValidation.ts (which TASK-003 reuses) is hand-rolled and key-by-key, so extra personA birth fields are silently ignored, not rejected, and nobody has chosen which is correct. Both fix sites are new files, so D5 permits it.
  - *narrowed:* Not "the ownership threat is untested" — 401, 404-on-foreign-ID and the profile-ID-only shape are covered. Narrower: the additive variant (a valid consented request that ALSO carries personA.birthDate/lat/lon/tz) has no case in TASK-003's validation matrix or TASK-007's route contract, and the reject-422-vs-explicit-drop behavior is undecided. Fix: one case in src/utils/relationship/relationshipInputValidation.test.ts, one mirroring case in src/server/app.relationship.test.ts — both new files.
- **coverage** — refuted: `False`. No existing mechanism closes it. No zod in package.json; validateBirthInput (src/utils/birthInputValidation.ts:66) picks known fields and never rejects unknown keys; no ESLint config (lint = tsc --noEmit, compile-time only); CI is deploy-only (.github/workflows/deploy-cloudrun.yml), no test job; zero tests mention profileId/personA. PRD F-006 (prd_report.md:350) is status source_inspected_and_planned, and plan:362/591 are prose — both excluded. app.ts:674 is the live raw-data precedent an implementer would copy.
  - *narrowed:* Narrower defect: no planned test pins the case where personA carries raw birth fields and NO profileId (or supplies both, leaving precedence unspecified). A fallback like `personA.profileId ? lookupOwned() : validateBirthInput(personA)` — the pattern already at src/server/app.ts:674 — passes all of TASK-007's tests (401, 404-on-foreign-ID, 422, call counts, rate limit, log redaction), since such a request never reaches the ownership lookup. Extra fields sent alongside a valid profileId are inert and not the gap.

---

## F-14 — [untested-failure-modes] (refutes 0/3)

RISK-007 / canvas R8 (DST shifts or duplicates generated samples) has a named test, TEST-009 (PRD:255) implemented at TASK-002 step 3 (plan:559), that lives entirely in the pure time-band module. Nothing tests that the resulting `adjusted` flag / `ambiguousTimePolicy` / `nonexistentTimePolicy` survives the BFF into `warnings` and reaches the UI. TASK-007's route test list (plan:686-691) contains no DST case; TASK-008 step 5 (plan:718) says "Preserve upstream precision/provenance and DST warnings" with no test attached; TASK-012 step 5 (plan:824) adds a method link "explaining … DST warnings" with no test in its acceptance criteria (plan:826-828). Scenario: `timeBands.ts` correctly computes `adjusted: true`, TEST-009 is green, and the transport mapper drops the field — the user sees a shifted sample presented as an ordinary one, which is exactly the failure REQ-NF-005 (plan:183) and AC-006 (plan:230-235) were written to prevent.

- **evidence** — refuted: `False`. All anchors verified verbatim: PRD:255, plan:559/686-691/718/824/826-828/183/230-235, canvas R8/RISK-007. TASK-002 binds TEST-009 to timeBands.test.ts only. Decisively, `timeResolution` appears once in 1096 plan lines (the transport JSON at :427) and in zero task steps, acceptance criteria or test lists; TASK-009 validates schema version, samples, statuses, errors only. Remaining DST mitigations (plan:456 prose, plan:1026 manual checklist, TEST-006 E2E) are non-automated or outside TASK-001..013 under D2.
- **consequence** — refuted: `False`. Live gap, not just documented. PRD:324 traces AC-006/TEST-009 to TASK-007 and TASK-008, yet TASK-007's steps (plan:686-691) have no DST case and TASK-008 step 5 has no test. AC-006 demands the warning be visible "im Transport" — something a pure timeBands unit test cannot assert. Fixable in-scope: relationshipTransport.ts, app.relationship.test.ts and TASK-012 component tests are all new files.
  - *narrowed:* Assert propagation at the new-file boundary only: a mapper test on src/server/relationshipTransport.ts proving adjusted/ambiguousTimePolicy/nonexistentTimePolicy reach `warnings`, plus a TASK-012 component test that the warning renders. Full route-level integration through src/server/app.ts stays untestable this run under D5 and must be recorded as a named residual gap rather than claimed under AC-006.
- **coverage** — refuted: `False`. No existing mechanism covers it. fufireClient.ts:186 / fufireClient.test.ts:190 cover only the upstream 422 dst_error rejection path, not adjusted-sample warning propagation. Overview.warnings.test.tsx:20 tests locally-generated normalizer warnings on a different pipeline. No zod, no safeParse/z.object anywhere in src/. Grep for "adjusted" finds nothing on timeResolution. PRD:304/324 traceability rows are matrix cells, which this lens excludes.
  - *narrowed:* The gap is real for the transport mapper (src/server/relationshipTransport.ts, a new file): no test asserts timeResolution.adjusted/policies or upstream DST warnings survive into the transport. The "reaches the UI" half is partly out of reach this run, since D5 puts App.tsx and PageShell.tsx out of scope, so scope the required test to the BFF/transport boundary plus the TASK-009 client guard.

---

## F-15 — [untested-failure-modes] (refutes 0/3)

The two mitigations that keep an unverified flow away from users — ROLLBACK-001 "Feature stays behind RELATIONSHIP_MVP_ENABLED" (PRD:279, plan:912,1034) and the compute-rate-limit inclusion (REQ-S-005 / TECH-INV-04) — are both prose with no falsifying test, and D5 makes them unwireable in this run. `RELATIONSHIP_MVP_ENABLED` appears in exactly three places across all docs and in zero task steps, zero ACs and zero tests; nothing defines its default or asserts it is off. Separately, the limiter is composed by path prefix at app construction, `app.use(["/api/azodiac","/api/gemini"], makeRateLimiter(...))` (src/server/app.ts:455), and `src/server/app.ts` is out of scope (canvas:272). The existing rate-limit proof imports `createApp` (src/server/app.ratelimit.test.ts:37); AC-010/TEST-010 never require the new proof to do the same, so a new `app.relationship.test.ts` that mounts its own express app would show 429 for a locally-composed router while `/api/relationships` is absent from the production limiter array.

- **evidence** — refuted: `False`. All anchors hold. app.ts:455 is verbatim `app.use(["/api/azodiac","/api/gemini"], makeRateLimiter(...))`; "relationship" appears nowhere in app.ts; canvas:272 lists app.ts as out of scope. RELATIONSHIP_MVP_ENABLED occurs only in prd_report.md:279, its json mirror, plan:912/1034 — zero src, task-step, AC or test hits, no default defined. TASK-007 step 5 and AC-010/TEST-010 never require createApp composition. Inference follows.
  - *narrowed:* Two citation nits, neither load-bearing: the flag occupies four doc lines (prd_report.json:1798 mirrors prd_report.md:279), and app.ratelimit.test.ts imports createApp at line 3 and instantiates it at lines 32/48, not line 37 (line 37 is inside that createApp-based test body).
- **consequence** — refuted: `False`. Acting on it changes in-scope artifacts. TASK-007/008 name existing src/server/app.ratelimit.test.ts as a test target; D5 puts it out of scope, and the canvas out-of-scope table (:265-275) omits it — an unlisted stop. AC-010 and REQ-S-005 become unprovable: a new test composing its own express app shows a false green, since the limiter binds by prefix at app.ts:455. The acceptance claim and the new test's shape both must change.
  - *narrowed:* The rate-limiter half is fully in-scope and actionable: mark AC-010/REQ-S-005 not-met for this run, surface app.ratelimit.test.ts as an unlisted D5 stop, and require any new src/server/app.relationship.test.ts to import createApp (as app.ratelimit.test.ts:3 does) and assert the true state rather than mounting a self-composed app. The RELATIONSHIP_MVP_ENABLED half is narrower — its plan hits are TASK-015 and the summary — but the run must still not report ROLLBACK-001 as a live mitigation, and a default-off flag guard is a permitted new file.
- **coverage** — refuted: `False`. No mechanism exists. RELATIONSHIP_MVP_ENABLED has 4 repo-wide hits, all prose docs — zero in src/ or any test, no default anywhere, and deploy-cloudrun.yml:39's --set-env-vars omits it. The limiter array at src/server/app.ts:455 is a literal; no test asserts its membership (app.ratelimit.test.ts:32,48 hits only /api/azodiac and /api/health), no eslint config exists, and the sole CI workflow is deploy-only, running no tests.
  - *narrowed:* Narrower: the global net at src/server/app.ts:451 (app.use("/api", makeRateLimiter(300))) does apply by prefix once a route is registered, so the risk is not "unlimited route" but "no compute-tier cap (20) per REQ-S-005/AC-010, and no test binding the proof to createApp()'s real composition." The ROLLBACK-001 half needs no narrowing — RELATIONSHIP_MVP_ENABLED is pure prose with zero code presence.

---

## F-16 — [internal-contradictions] (refutes 0/3)

The PRD and the implementation plan each define a set of acceptance criteria numbered AC-001…AC-0xx, with DIFFERENT meanings for the same IDs, and CTX-002 declares both artifacts source-of-truth. PRD AC-006 = "New flow contains no score or relationship verdict" (prd_report.md:237) while plan AC-006 = "DST edge behavior" (plan:230). PRD AC-008 = "Depth work begins only after the real MVP evidence gate" (prd_report.md:239) while plan AC-008 = "No score" (plan:244). PRD AC-005 = real-boundary flow (prd:236) while plan AC-005 = unknown time (plan:223). The PRD's own traceability matrix already silently mixes both schemes: row `ARCH-004 | AC-008 | TEST-007` (prd:325) maps the compareProfiles prohibition to AC-008, which is correct only under PLAN numbering and flatly wrong under the PRD's own §20 numbering ten lines above. Row `NFR-001 | AC-008` (prd:310) maps responsive-viewport to a no-score/depth-gate criterion under either scheme.

- **evidence** — refuted: `False`. Every anchor opened and matched verbatim: prd 236/237/239, plan 223/230/244, CTX-002 at prd:197, and rows prd:310/325. ARCH-004 (prd:177, compareProfiles prohibited) with TEST-007 (prd:253, compareProfiles static check) maps to AC-008, coherent only under plan numbering. Stronger still: prd:306 maps the identical requirement/test/tasks to AC-006, and prd:311 maps the depth gate to AC-008 — contradiction internal to the PRD. prd_report.json is identical.
  - *narrowed:* Stands as written except the parenthetical "ten lines above": §20's AC list is at prd_report.md:232-244, roughly 80 lines above the matrix row at 325, not ten. Strengthen it by citing prd:306 (FR-007 | AC-006 | TEST-007 | TASK-001, TASK-011, TASK-012) against prd:325 (ARCH-004 | AC-008 | TEST-007 | same tasks) — same requirement, same test, same tasks, two AC IDs — plus prd:311 (FR-009 | AC-008), making the contradiction provable from the PRD alone, and note prd_report.json carries it identically.
- **consequence** — refuted: `False`. Verified: PRD and plan use incompatible AC-005/006/008 meanings while CTX-002 (prd:197) makes both source-of-truth. PRD's own matrix mixes schemes — ARCH-004|AC-008 (prd:325) vs FR-007|AC-006 (prd:306) share TEST-007 and TASK-001/011/012; NFR-001|AC-008 (prd:310) is wrong under both. Affected tasks 001/011/012/013 are all in TASK-001..013. The no-score test's evidence record must cite an AC ID, and citing plan AC-008 records green against PRD AC-008, which D2 guarantees is unmet.
  - *narrowed:* Before writing TASK-001/011/012/013 evidence, namespace the two schemes (PRD-AC-xxx vs PLAN-AC-xxx) or adopt one, and correct prd_report.md:325 (ARCH-004 → AC-006) and :310 (NFR-001 → a responsive criterion). Every new-file DoD/evidence record in this run must cite the namespaced ID so no acceptance claim is booked against PRD AC-008.
- **coverage** — refuted: `False`. No mechanism exists. scripts/validate_pack.py (36 lines) checks only file existence and substring presence — it never parses AC IDs or the traceability matrix. Lint is tsc --noEmit (blind to markdown); the sole CI workflow only deploys; no husky hooks; grep for "AC-0" across src/ and tests/ returns zero hits. The contradiction is verified in-file: ARCH-004 (prd:177, compareProfiles ban) maps to AC-008 (prd:325), which prd:239 defines as the depth gate, while identical FR-007 (prd:141) maps to AC-006 (prd:306) — same TEST-007, two IDs.
  - *narrowed:* Finding stands as written; every cited line verified. Strongest evidence is intra-PRD, needing no plan comparison: ARCH-004→AC-008 (prd:325) and FR-007→AC-006 (prd:306) trace the same no-score requirement via the same TEST-007 to two different IDs, and NFR-001→AC-008 (prd:310) maps responsive-viewport to a criterion wrong under both schemes.

---

## F-17 — [internal-contradictions] (refutes 0/3)

For `unknown` time the sampling anchors are 00:00, 12:00 and 23:59 (plan:485, plan AC-005:226), and `provisional` is defined as requiring the aspect to hold in the CANONICAL CENTER PAIR plus ceil(totalPairs/2) (plan:492, PRD TERM-002:102, DEC-03). The canonical center for `unknown` is the 12:00 sample. So no signal from an unknown-time person can ever be shown unless it survives at exactly 12:00 — the placeholder value the PRD's own problem statement declares must not be treated as privileged (INFO-003, prd:83; F-002, prd:346; canvas §1.2 'Die stille 12:00'). 12:00 is handed a silent veto over every non-stable claim, i.e. the strongest possible epistemic status, while DOD-003 (prd:288) forbids any 'placeholder' from supporting the completion claim.

- **evidence** — refuted: `False`. Every anchor verifies verbatim (plan:485, 226, 492; TERM-002:102; DEC-03; INFO-003:83; F-002:346; canvas:35; DOD-003:288). The inference is arithmetic: steps 7-9 apply to unknown, midpoint of {00:00,12:00,23:59} is 12:00, ceil(3/2)=2, so 00:00+23:59 yields unavailable while 12:00+one endpoint yields provisional. 12:00 is a necessary condition; the other equally arbitrary anchors are not. Asymmetry undocumented.
  - *narrowed:* For `unknown`, the canonical-center rule (imported unchanged from `approximate`, where the midpoint represents a user-selected band) makes the arbitrary 12:00 anchor a necessary condition for `provisional`: a signal holding at 00:00 and 23:59 is suppressed as `unavailable` while one holding at 12:00 plus either endpoint surfaces. This privileges exactly the placeholder INFO-003, F-002 and canvas 1.2 flag, with no stated rationale for why 12:00 deserves veto power the other two anchors lack. Drop the DOD-003 clause — its 'placeholder' sits in a list of fake-implementation artifacts and the tie-in is a stretch.
- **consequence** — refuted: `False`. TASK-010 (plan:756, new files only, in scope) implements the center-gate. For `unknown` the center IS 12:00, so the gate is decisive, yet plan:767 specifies no test for "aspect at 00:00+23:59, absent at 12:00" and AC-005 (plan:226) omits AC-004's "midpoint is not a measured birth time" clause. Acting adds a named test case and an AC-005 clause. Untested decisive rule, not merely documented.
  - *narrowed:* Narrower defensible version: drop the DOD-003 / "strongest epistemic status" framing (a sampling anchor is not the DOD-003 "placeholder", and requiring the center is a legitimate anti-edge-artifact heuristic). Keep: (1) for `unknown`, the canonical center pair is the 12:00 placeholder, making it a silent veto on every provisional signal; (2) that veto is unspecified in AC-005 and has no named test in TASK-010 step 1/2; (3) AC-004 carries a "midpoint is not a measured birth time" guard that AC-005 lacks, even though unknown is the case where the midpoint is exactly the value F-002/INFO-003 flags. Fix inside this run: add an explicit `sampleStability.test.ts` case asserting the 00:00+23:59-without-12:00 outcome and its reason code, and either justify the center-gate for `unknown` in DEC-03 or branch it per dataPrecision.
- **coverage** — refuted: `False`. Not covered. No sampling/stability code or test exists — find over src/ and tests/ for relationship|sampl|stability|time-band returns nothing. The only honesty guard, src/__tests__/synastryWording.test.ts:29-77, scans P7 German verdict words, not classifier logic. Remaining mitigations are prose: plan:1024 manual checklist, ULTRATHINK:27, EVIDENCE_DIALECTIC:52. Worse, plan:558 and plan:767 instruct tests that assert the center-pair conjunct, entrenching it. Facts verified at plan:487, plan:492, prd:102, decisions:27.
  - *narrowed:* Spec defect, not a DOD-003 violation. `provisional` is documented as coverage-based (ceil(totalPairs/2)) but carries an unstated single-cell veto: with both persons `unknown`, 9 pairs, a signal holding in the 6 off-diagonal cells (6/9 ≥ 5) is still demoted to `unavailable` because the noon×noon pair misses it. The 12:00 conjunct is fail-closed (it only suppresses, never presents 12:00 as exact) and 12:00 is the genuine day-midpoint, so INFO-003/F-002/DOD-003 are not breached. The defensible claim: plan:492 / prd:102 / decisions:27 describe a coverage threshold while implementing coverage AND a mandatory center cell; no repository mechanism makes that discrepancy visible, and plan:558/767 lock it in. Fix = state and justify the conjunct explicitly (or drop it for `unknown`, where no user-asserted interval exists) plus a test pinning a high-coverage/center-miss case.

---

## F-18 — [internal-contradictions] (refutes 0/3)

The number of patterns the result screen must show is specified four incompatible ways. Plan acceptance line 29 and REQ-F-008 (plan:166) say 'mindestens drei' — at least three. PRD FR-006 (prd:140) says 'up to three patterns'; the confirmed canvas §4 says 'Bis zu drei Muster'. TASK-011's own acceptance says 'zero to three evidence-linked patterns, never invented filler' (plan:799) and step 5 mandates 'Return honest Missing when fewer than three acceptable patterns exist' (plan:796). TASK-014 step 4 asks for 'at least one computed pattern or an honest Missing-State' (plan:873). ≥3, ≤3, 0–3 and ≥1 cannot all hold. A component or E2E test written from REQ-F-008 fails on the exact honest-Missing path TASK-011 requires; a test written from TASK-011 fails REQ-F-008.

- **evidence** — refuted: `False`. Every anchor quotes verbatim and correctly. plan:29 and REQ-F-008 (plan:166) say "mindestens drei"; canvas §4 (:84) and PRD FR-006 (:140) say "bis zu drei"/"up to three"; TASK-011 (:796,:799) mandates fewer-than-three and 0-3. Opposite quantifier bounds on the same object. Stronger than claimed: TASK-011's own objective (:784, "up to three") contradicts the requirement it links at :785. Inference holds.
  - *narrowed:* The pattern count is specified in two directly opposed ways: at least three (plan:29, REQ-F-008 plan:166) versus up to three (canvas §4:84, PRD FR-006:140, TASK-011 objective plan:784, acceptance plan:799, step 5 plan:796). TASK-011 links REQ-F-008 at plan:785 while its own objective states the opposite bound. A component test derived from REQ-F-008 fails the honest-Missing path TASK-011 mandates. Drop the TASK-014 ":873" limb — "at least one" is a weaker assertion, not an incompatible fourth spec.
- **consequence** — refuted: `False`. Verified verbatim: plan:29/166 "mindestens drei", prd:140 "up to three", canvas:84 "Bis zu drei", plan:799 "zero to three". TASK-011 is in scope and src/utils/relationship/ does not exist yet, so the fork is unmade. It decides the analyzer's return shape (fixed three slots vs variable 0–3), the assertion in the new relationshipAnalyzer.test.ts, and whether acceptance line 29 can be truthfully claimed. Padding to three would manufacture filler.
  - *narrowed:* The live conflict is ≥3 (plan:29, REQ-F-008) versus 0–3 (TASK-011:799) plus ≤3 (prd:140, canvas:84). Resolve it before writing src/utils/relationship/relationshipAnalyzer.ts: adopt 0–3 evidence-linked patterns with honest Missing as canonical (it is the only reading consistent with the anti-fabrication rule), assert that in the new test, and correct plan:29/166 — or explicitly define "mindestens drei" as three slots each holding a pattern or a Missing-State. The TASK-014 "at least one" leg (plan:873) is out of scope for this run and can be ignored.
- **coverage** — refuted: `False`. No covering mechanism exists. src/utils/relationship/ and src/content/relationship/ are absent (no analyzer, no test); npm run lint is only `tsc --noEmit` (package.json:11), which cannot catch a prose count conflict; the sole CI workflow is deploy-cloudrun.yml; no spec/traceability linter in scripts/; no western-synastry contradictions doc; REQ-F-008 and FR-006 appear nowhere but plan:166 and prd:140. All cited lines verified accurate.
  - *narrowed:* Narrower defensible version: REQ-F-008 ("mindestens drei ... Muster oder ehrliche Missing-States") can be read as three display slots each filled by a pattern OR an honest Missing, which does reconcile with TASK-011's "zero to three patterns" (plan:796/799) and with the canvas/PRD "bis zu drei" ceiling. So it is not four irreconcilable numbers. The real, uncovered defect: that reconciling reading is nowhere stated normatively, so TASK-011's test author must guess whether "three" is a floor on displayed items or a cap on patterns; and TASK-014 step 4 (plan:873) sets a third, weaker bar ("at least one computed pattern or an honest Missing-State") that no reading of REQ-F-008 produces. Resolve by fixing one normative sentence (canvas §4 is the confirmed baseline) before TASK-011 — in scope, since relationshipAnalyzer.test.ts is a new file under D5.

---

## F-19 — [internal-contradictions] (refutes 0/3)

AC-012 states without qualification that 'storage calls are absent from the relationship flow' (prd:242). FR-001 (prd:135) and TASK-004 (plan:600, step 2 at :613, acceptance at :620) require the user to be able to CREATE an owned profile from inside that flow, in `src/components/relationship/ProfileStep.tsx`, via POST /api/me/profiles — which is a row INSERT into nb_profiles (verified at src/server/app.ts:831-852). The two cannot both hold. TEST-011 (prd:257) quietly resolves the conflict by testing only the narrower claim ('does not write partner input or analysis result'), which means AC-012's second clause is asserted in the PRD and verified nowhere — it survives only by being weakened into a different, weaker statement.

- **evidence** — refuted: `False`. All anchors verified verbatim: AC-012 (prd:242) says "storage calls are absent from the relationship flow" unqualified; FR-001 (prd:135) and TASK-004 (plan:600/613/620) mandate profile CREATION from src/components/relationship/ProfileStep.tsx via POST /api/me/profiles, which INSERTs into nb_profiles (app.ts:831-852). "Relationship flow" is defined nowhere — grep finds it only in AC-012 and TEST-011 — so nothing scopes the profile step out. TEST-011 (prd:257) verifies only clause 1 verbatim; the trace matrix (prd:315) gives clause 2 no other test.
  - *narrowed:* AC-012's second clause is overbroad rather than a contradiction of intent: the trace matrix (prd:315) maps AC-012 to FR-002 (partner data), suggesting "storage calls" was meant as "partner-data storage calls." But as written the clause is unqualified and false for the mandated design, and its literal form is verified by no test — TEST-011 silently substitutes the narrower clause 1. Fix is to scope AC-012's wording to partner input and analysis result, matching what TEST-011 actually proves.
- **consequence** — refuted: `False`. Verified: app.ts:848 INSERTs into nb_profiles, and FR-001/TASK-004 mandate that create path inside src/components/relationship/ProfileStep.tsx. AC-012's second clause is unqualified and falsifiable as written. Worse than reported: grep shows NO task implements TEST-011 or REQ-D-004's DB-call spy; the matrix routes it to out-of-scope TASK-015. Acting changes an acceptance claim plus new-file tests — both inside D5 scope.
  - *narrowed:* AC-012's second clause ("storage calls are absent from the relationship flow") is over-broad versus REQ-D-004's correct narrower claim (partner profile and result are not persisted), and is contradicted by the FR-001/TASK-004 profile-create INSERT. Additionally, no task in TASK-001..013 owns TEST-011 or the DB-call spy at all. Fix in-scope: narrow AC-012 to REQ-D-004 wording, and add a storage-spy assertion to the new ProfileStep.test.tsx / PartnerStep.test.tsx.
- **coverage** — refuted: `False`. Contradiction confirmed and worse than reported: AC-005 (prd:236) requires "actual authenticated profile persistence" while AC-012 (prd:242) forbids storage calls — both accepted p0. FR-001 (prd:135) and TASK-004 (plan:600,620) mandate profile creation, which INSERTs at app.ts:831-852. No coverage exists: no relationship files, lint is tsc, test is vitest, the sole workflow only deploys, no PRD checker. Traceability row prd:315 is prose, excluded by this lens.
  - *narrowed:* AC-012's second clause must be scoped to the partner-capture and analysis path, with user-initiated owned-profile creation via POST /api/me/profiles named as an explicit exception (matching AC-005 and REQ-D-004 at plan:173). Fix the AC text to match TEST-011's actual scope rather than leaving the broader claim asserted and untested.

---

## F-20 — [internal-contradictions] (refutes 1/3)

SEC-106 (prd:379) requires rejecting 'excessive sample fan-out ... before upstream calls' as a control, and F-007 (prd:351) names the impact as 'One request can fan out to six paid upstream calls'. The only mechanism specified anywhere to discharge this is the existing compute rate limiter (API-007 prd:170, SEC-006 prd:187, AC-010 prd:241, TEST-010 prd:256, TECH-INV-04). That limiter counts REQUESTS, not upstream calls: src/server/app.ts:57-68 is a plain express-rate-limit with `limit` = RATE_LIMIT_COMPUTE_MAX (default 20) applied at :455. Adding /api/relationships to it therefore permits 20 requests × 6 FuFirE calls = 120 upstream calls per window from one IP, versus 20 × 2 = 40 for the legacy /api/azodiac path sharing the same bucket. AC-010/TEST-010 can go green (429 is genuinely returned) while the fan-out exposure F-007 identifies is entirely untouched — the control satisfies the letter of API-007 only by weakening SEC-106's fan-out clause into uselessness.

- **evidence** — refuted: `True`. Anchors verify verbatim, but the load-bearing claim "the only mechanism specified anywhere" is false against the PRD itself: NFR-003 (prd:150) and SUS-001 (prd:191) specify a dedicated per-request cap of three samples/person, six calls/request, tested at plan:689 and asserted at plan:1038. SEC-106 lists fan-out and rate-limit violations as separate clauses with separate controls; the finding conflates them, so the "weakened into uselessness" inference does not hold.
  - *narrowed:* The new route and legacy /api/azodiac (synastry = 2x /chart, app.ts:485) share one RATE_LIMIT_COMPUTE_MAX=20 bucket despite up to 6 upstream calls each, giving the new path ~3x the per-window upstream cost ceiling (120 vs 40). The PRD never re-sizes the shared threshold for this asymmetry. That is an unaddressed calibration gap, not a contradiction or a defeated control.
- **consequence** — refuted: `False`. Not refuted: it changes TASK-007/008. Under D5 app.ts is untouched, so the new route's limiter is written fresh in a new file — its counting unit and threshold are an unavoidable in-run choice, and AC-010's completion claim needs narrowing. But the premise is wrong: SEC-106 lists sample fan-out separately from rate-limit violations, and the ≤3/person, ≤6/request cap (SUS-001, REQ-NF-003) is already asserted by TASK-007 step 4.
  - *narrowed:* Drop the "internal contradiction" and "only mechanism" framing — the per-request fan-out cap exists (SUS-001, REQ-NF-003) and is tested in TASK-007 step 4. The defensible residue is narrower: (a) no control anywhere bounds AGGREGATE upstream calls per IP per window, and a 6x-cost route inheriting the legacy RATE_LIMIT_COMPUTE_MAX=20 bucket has no test, guard, or owner; (b) since D5 forces the relationship limiter into a new file, give it a route-specific lower max (e.g. RATE_LIMIT_RELATIONSHIP_MAX, sized for call-cost parity with /api/azodiac) rather than reusing 20; (c) word the AC-010/TEST-010 completion claim as "429 on request count" and explicitly record aggregate fan-out exposure as undischarged, not as SEC-106 satisfied.
- **coverage** — refuted: `False`. No existing mechanism closes it. app.ts:56-71/:455 counts requests only; app.ratelimit.test.ts asserts 429 on the 3rd POST and never counts upstream calls; fufireClient.ts has zero budget/quota/cache hits; no CI check. The only fan-out cap, REQ-NF-003 (plan:181), is unimplemented planned work and caps per-request (the 6x the finding assumes), not aggregate per-IP upstream spend.
  - *narrowed:* Narrower: REQ-NF-003 (plan:181) does specify a per-request 3-calls-per-person cap, so the compute limiter is not the only specified mechanism. The surviving, uncovered claim: nothing in the repo or spec bounds aggregate upstream FuFirE calls per IP, so sharing the 20-request compute bucket lets /api/relationships cost 6x per token (120 calls/window) versus legacy /api/azodiac, and AC-010/TEST-010 can pass without detecting it.

---

## F-21 — [internal-contradictions] (refutes 0/3)

The design token table assigns `--rel-peach #BB8588` the role 'Fokus und Interaktion' (plan:143), while the design rules eleven lines later require 'Pastellflächen erhalten dunkle Text- und Fokustokens' (plan:153). #BB8588 is not a dark token: measured against `--rel-bg #EFEBCE` it is 2.56:1 and against `--rel-surface #F8F5E5` it is 2.81:1, both below the 3:1 WCAG 2.2 SC 1.4.11 threshold for focus indicators and UI component boundaries. NFR-002 (prd:149) requires 'visible focus', TASK-013 step 3 requires 'visible focus rings' and its acceptance requires the pairs to meet 'the agreed AA threshold' (plan:851), verified by the new src/__tests__/relationshipContrast.test.ts. The token table and the accessibility requirement cannot both be implemented as written.

- **evidence** — refuted: `False`. All anchors verified verbatim: plan:143 (peach = "Fokus und Interaktion"), plan:153 (pastel surfaces get dark focus tokens), prd:149, plan:848, plan:852. Contrast recomputed independently: 2.557 and 2.811, both under 3:1. The conflict follows: peach is neither dark nor 3:1-compliant on either pastel surface, so the token role and the design rule cannot both be honored literally. Two trivial imprecisions do not touch the substance.
  - *narrowed:* Narrower defensible version: the palette table and design rule are in unresolved conflict over what colors the focus indicator, not a proven impossibility. "Fokus und Interaktion" can be read as a usage-domain label, so a compliant implementation could tint interactive fills with peach while drawing the ring in --rel-olive-strong (6.12:1) or --rel-text (12.03:1). Also: plan:153 is ten lines after 143, not eleven; the acceptance quote sits at plan:852 (851 is the header). Compounding the defect, "the agreed AA threshold" is never numerically defined in the plan or PRD — no 3:1, 4.5:1, or WCAG citation exists in either document — so TASK-013's acceptance criterion and its relationshipContrast.test.ts have no stated pass bar. The token's focus role and the numeric threshold both need resolution before TASK-013 is implemented.
- **consequence** — refuted: `False`. Confirmed by measurement: 2.56:1 and 2.81:1, both under 3:1. TASK-013 is in scope and both affected files are new, so acting changes real work: relationship.css must not bind :focus-visible to --rel-peach (use --rel-olive-strong, 6.12/6.73), and relationshipContrast.test.ts needs a 3:1 non-text tier. The existing contrast.test.ts tests only 4.5:1 text, so a test modeled on it would pass while focus stays invisible.
  - *narrowed:* The contradiction is scoped to the focus-indicator role, not the whole token. #BB8588 as a fill with --rel-text on it is 4.71:1 and compliant, so "Interaktion" survives. What cannot be implemented as written is #BB8588 as a focus ring or UI-component boundary against --rel-bg/--rel-surface (2.56/2.81 < 3:1, WCAG 2.2 SC 1.4.11). Fix inside TASK-013: bind focus rings to --rel-olive-strong, keep peach decorative, and pin "the agreed AA threshold" to explicit numbers (4.5:1 text, 3:1 focus/non-text) with a test tier for each.
- **coverage** — refuted: `False`. Math confirmed via repo's own contrastRatio: #BB8588 is 2.56:1 on --rel-bg, 2.81:1 on --rel-surface, both under the 3:1 SC 1.4.11 bar. No existing mechanism covers it: contrast.test.ts:6 is hardcoded to OBSIDIAN #050505 with a gold/stone APPROVED list and never touches pastel tokens; lint is tsc --noEmit; deploy-cloudrun.yml runs no tests; axe is not a dependency; relationshipContrast.test.ts does not exist yet.
  - *narrowed:* The contradiction is real but resolvable rather than unimplementable: peach can stay a decorative interaction fill while focus rings switch to --rel-olive-strong (6.12:1/6.73:1) or --rel-text, or use a two-tone ring. So the defect is that plan:143 assigns --rel-peach the focus/interaction role in conflict with plan:153 and NFR-002, forcing an undecided design choice — and the plan's "agreed AA threshold" (plan:851) is never given a numeric value, so TASK-013's test cannot be written unambiguously.

---

## F-22 — [internal-contradictions] (refutes 0/3)

Two unconditional error contracts overlap and disagree. API-006 (prd:169) and AC-002 (prd:233): 'Missing, malformed or foreign personAProfileId returns 404 and zero FuFirE calls.' API-002 (prd:165) and AC-001 (prd:232): 'Missing or false consent returns 422 before any FuFirE call.' TASK-003 step 3 (plan:587) then mandates an order — 'missing/false consent and malformed input short-circuit BEFORE profile lookup' — which forces 422 for a request that is both consent-less and carries a foreign ID, contradicting API-006's unconditional 404. TASK-008's implementation step list (plan:713-720) mandates the opposite order: step 2 is the owner-filtered profile load returning 404, and the consent gate does not appear as a step at all, despite REQ-S-002/AC-001 being TASK-008's stated requirement link. TASK-007 likewise writes the 404 tests (step 2) before the 422 tests (step 3).

- **evidence** — refuted: `False`. Primary anchors verify verbatim at cited lines: prd:165, 169, 232, 233; plan:585 (cited 587, trivial offset); plan:713-720. The core inference holds — API-006's unconditional 404 and TASK-003 step 3's consent-first ordering give contradictory results for a consent-less request bearing a foreign ID, leaving AC-001/AC-002 jointly untestable. One subordinate anchor is wrong (REQ-S-002 is TASK-007's link, not TASK-008's), but the defect does not rest on it.
  - *narrowed:* API-002/AC-001 (422 on missing consent) and API-006/AC-002 (404 on foreign profile ID) are both stated unconditionally with no precedence rule. TASK-003 step 3 (plan:585) resolves the overlap toward 422, which contradicts API-006's unconditional 404 for a request that is both consent-less and carries a foreign ID. TASK-008 compounds this: its acceptance criterion (plan:722) requires "Missing consent or foreign profile never reaches FuFirE," yet none of its seven steps invokes the consent validator, and its first data-touching step (plan:715) is the 404 lookup. Drop two sub-claims: TASK-008's requirement links (plan:706) contain REQ-S-001, not REQ-S-002 — AC-001 traces to TASK-008 only via the PRD matrix (prd:307); and TASK-007's 404-before-422 test-authoring order carries no runtime-precedence meaning. Fix by adding an explicit precedence clause to the PRD.
- **consequence** — refuted: `False`. Real, unresolved contradiction. AC-001 (422 unconditional) and AC-002 (404 unconditional) both fire on one request (no consent + foreign ID); only one status can return. Neither decisions doc nor canvas states precedence (checked both). This changes work inside TASK-001..013 and inside D5's new-files-only scope: TASK-003's new validator encodes step-3 consent-first ordering, while TASK-008's step list puts profile-404 first and omits the consent gate — so TASK-007's new app.relationship.test.ts must assert a precedence it currently has no basis for, and the AC-001/AC-002 pass claim is unverifiable until one is declared subordinate.
  - *narrowed:* Narrower defensible claim: precedence between the 422 consent gate and the 404 owner-filtered lookup is undeclared for the overlapping request. Pick one (TASK-003 step 3 already implies consent-first), restate AC-001/AC-002 as conditional rather than unconditional, add an explicit test in the new src/server/app.relationship.test.ts for consent-less + foreign ID asserting the chosen code and zero FuFirE calls, and realign TASK-008's step order to match. The TASK-007 test-authoring order (404 tests before 422 tests) is cosmetic and not itself a defect.
- **coverage** — refuted: `False`. No mechanism exists. The route, validator and app.relationship.test.ts are unwritten; `lint` is bare `tsc --noEmit` (package.json:11) and the sole workflow (deploy-cloudrun.yml) runs no spec gate. TASK-007's tests (plan:687-688) cover foreign-ID and consent-less separately, never combined, so nothing pins precedence. Verified: TASK-008's steps (plan:713-720) contain no consent step — consent appears only as a prose acceptance bullet (plan:724), exactly the mitigation class this audit excludes.
  - *narrowed:* The consent-less + foreign-ID overlap is partly disambiguated: plan:193 scopes AC-001's 422 to "ein eigenes Profil", implying 404 governs that combination. The unscoped, undeniable contradiction is malformed profileId: TASK-003 (plan:583,585) validates "invalid profile UUID" pre-lookup as a 422 candidate, while API-006 (prd:169) and transport rule plan:451 both demand 404. Independently solid: TASK-008 (plan:713-720) omits the consent gate from its implementation steps entirely, leaving the 422-before-404 ordering unimplemented and untested.

---

## F-23 — [internal-contradictions] (refutes 0/3)

TASK-001, the first task of the run, lists as 'Modify/Test: existing route tests plus src/__tests__/synastryWording.test.ts' (plan:529) and step 1 requires adding a characterisation test recording the current score presence. The user-confirmed canvas puts that exact file in 'Ausdrücklich nicht im Scope (auch nicht auf Rückfrage im MVP)' (canvas:284-285) under decision D5 and the R5-a resolution, and the allowed change scope permits new files only (canvas:244-248). TASK-011's validation command still runs and depends on the same file (plan:803, 'Expected result: product analyzer and anti-score tests pass'). So the run's first task is unexecutable as written, and the canvas's own 'Bekannte, eingeplante Scope-Stops' table (canvas:270-275) — built precisely so interruptions are predictable — does not list it, meaning this stop arrives unannounced.

- **evidence** — refuted: `False`. Every anchor checks out verbatim: plan:529 names synastryWording.test.ts as Modify/Test; canvas:244-248 confirms new-files-only, hard stop on any existing file; canvas:284-285 puts that file out of scope even on request; canvas:270-275 omits it from the announced scope-stop table. Both named targets exist on disk. TASK-001 therefore cannot run as written, and the stop is unannounced and unapprovable.
  - *narrowed:* TASK-001's Modify/Test targets (src/server/app.test.ts, src/__tests__/synastryWording.test.ts) are existing files barred by the confirmed new-files-only scope, the latter unapprovable even on Rückfrage; the run's first task is thus unexecutable as written and the stop is absent from the canvas's pre-announced scope-stop table. Drop the TASK-011 sub-claim: its validation command merely runs the file, which is read-only and explicitly planned reuse (canvas:209), not a scope conflict.
- **consequence** — refuted: `False`. Verified: plan:529 marks two existing files as Modify. canvas:284 bans synastryWording.test.ts outright; src/server/app.test.ts (exists, holds the /api/azodiac/synastry describe) appears nowhere in the canvas — not allowed, not an announced stop. Canvas R5-a itself says the compareProfiles guard belongs in the new relationshipNoScore.test.ts, contradicting TASK-001 step 3. Acting on it rewrites TASK-001's file list and steps.
  - *narrowed:* TASK-001 names two out-of-scope existing files (src/server/app.test.ts, src/__tests__/synastryWording.test.ts) as Modify targets; neither appears in the canvas's announced scope-stops table, so the run's first task hits an unannounced hard stop. Steps 1 and 3 must be re-targeted to allowed new files (src/server/app.relationship.test.ts, src/__tests__/relationshipNoScore.test.ts), per the canvas's own R5-a resolution. TASK-011's leg is weaker: it only runs synastryWording.test.ts read-only, which is not a scope violation, though its validation command should point at relationshipNoScore.test.ts for consistency with R5-a.
- **coverage** — refuted: `False`. Verified: plan:529 names two frozen files; canvas:284 forbids one, canvas:244 allows new files only; the scope-stop table (canvas:270-279) omits TASK-001. Existing coverage is real but partial: plumbline-scope-check (run: EXIT 3 on exactly these files) blocks silent breaches, yet it is manual (.claude/commands/pril-check.md:31), not CI. It cannot fix the contradiction. Worse: synastryWording.test.ts has zero score assertions, so TASK-011's anti-score validation passes vacuously.
  - *narrowed:* TASK-001 cannot execute as written: plumbline-scope-check hard-stops on both files it names, and that stop is missing from the canvas scope-stop table built for predictability. Consequence is not a silent violation (the guard prevents that) but a vacuously-green gate: synastryWording.test.ts contains no score assertions, so TASK-011's "anti-score tests pass" proves nothing. Fix by routing the no-score proof to the already-allowed new file src/__tests__/relationshipNoScore.test.ts (canvas:257) and adding TASK-001 to the scope-stop table.

---

## F-24 — [wired-in-prod] (refutes 0/3)

Under D5 the only way TASK-007/008 contract tests can go green is for the test file to assemble its own Express app around a new-file router; those tests then pass permanently while createApp() still 404s, and nothing in the artifact set forces the test to touch the composed app. I verified empirically: `createApp()` + supertest POST /api/relationships/western-synastry -> 404. REQs/ACs that would be reported satisfied while unreachable: PRD API-001, API-002, API-006, FR-005, NFR-003, SEC-004, AC-001, AC-002, AC-003, AC-004, AC-012; plan REQ-S-002, REQ-S-004, REQ-F-005, REQ-F-006, REQ-A-002 and plan AC-001/AC-002/AC-003/AC-004. Named tests that still pass: src/server/app.relationship.test.ts (the sole allowed-scope route test, canvas:258), plus PRD TEST-001, TEST-004, TEST-011.

- **evidence** — refuted: `False`. Every anchor checks out. No relationships route exists in src/; I re-ran the probe: createApp() + supertest POST /api/relationships/western-synastry returns 404. Canvas:257-258 allows only relationshipTransport.ts and app.relationship.test.ts; app.ts is a scope-stop. TASK-007 (plan:677-694) requires red tests against the missing route while app.ts stays untouched, so a test-local Express assembly is the only D5-legal green path. All cited PRD/plan IDs exist and say what is claimed; traceability rows 307/317/321 route them through TASK-007/008.
  - *narrowed:* Same finding, minus two items: REQ-F-006's evidence class is "real-boundary smoke", already foreclosed by D2, so it would not be claimed on this ground; and AC-003/AC-004 have frontend-side classification content genuinely reachable in new files — only their BFF sampling halves are unreachable.
- **consequence** — refuted: `False`. Verified: createApp() 404s on POST /api/relationships/western-synastry. Acting changes this run's output. TASK-008's AC "contract, authz and rate-limit tests pass" would be reported satisfied by a self-assembled test app. Worse, the compute limiter is path-mounted at app.ts:455, so REQ-S-004/SEC-004's 429 proof is unprovable in scope, not merely unwired. A new-file sentinel asserting the 404 today makes the gap fail loudly on release.
  - *narrowed:* The unreachability itself is anticipated by the canvas (TASK-008 stop, canvas ~line 283), so the finding is not a discovery of an unknown gap. The defensible residue, all actionable inside TASK-001..013 under D5: (a) TASK-008 and REQs API-001/002/006, FR-005, NFR-003, SEC-004, AC-001..004, AC-012, REQ-S-002/004, REQ-F-005/006, REQ-A-002 must be reported implemented-but-unreachable, never satisfied; (b) src/server/app.relationship.test.ts should carry a sentinel asserting createApp() 404s today, converting a prose limitation into a guard that fails when app.ts is released; (c) the rate-limit AC should be carved out explicitly as unprovable in scope, since the limiter is a path-mounted middleware in the out-of-scope file and no new-file test can evidence it.
- **coverage** — refuted: `False`. Verified empirically: createApp() 404s on POST /api/relationships/western-synastry (probe test passed, then removed). Lens 3 finds no closing mechanism: no composed-app route-manifest or contract test exists (app.test.ts has none); `npm run lint` is only `tsc --noEmit`, which happily type-checks an unmounted router; the sole CI workflow (.github/workflows/deploy-cloudrun.yml) runs no tests at all; D2 excludes the e2e real-boundary spec. The plan's Scope-Stops table is prose. plumbline-scope-check enforces the gap, it does not cover it.
  - *narrowed:* Finding stands as written. Minor sharpening: the certainty is unreachability, not necessarily a green test — under honest execution TASK-007 tests may simply stay red, since TASK-008 mandates modifying src/server/app.ts (plan lines 705, and the compute limiter at app.ts:455) which D5 forbids. Either way API-001/002/006, FR-005, NFR-003, SEC-004 and their ACs are unsatisfiable via the composed app, and NFR-003's rate-limit clause is unreachable by any self-assembled test app.

---

## F-25 — [wired-in-prod] (refutes 0/3)

AC-010/REQ-S-005 can be reported satisfied by a rate-limit test that proves nothing about the compute cap, in two independent ways. (a) /api/relationships/western-synastry already matches the GLOBAL limiter (app.use("/api", …), default 300) — I confirmed this empirically — so a test asserting 'eventually 429' proves the 300-cap, not the intended 20-cap; at 300 POSTs x 6 upstream calls that is ~1800 FuFirE calls per window, defeating NFR-003/SUS-001. (b) rateLimitSkipped() returns true under NODE_ENV=test and the limits are read at createApp() time, so a new test file that does not set RATE_LIMIT_FORCE=true and RATE_LIMIT_GLOBAL_MAX high BEFORE createApp() exercises no limiter at all and passes vacuously.

- **evidence** — refuted: `False`. All anchors verified. app.ts:451 mounts the 300-default global limiter on /api; app.ts:455 mounts the 20-cap on /api/azodiac + /api/gemini only, excluding /api/relationships. rateLimitSkipped() (app.ts:51-53) is true under NODE_ENV=test; maxima are read at createApp(). Probes confirmed: 40 POSTs to the new path never 429 without env; COMPUTE_MAX=1 does not bite there but does on /api/azodiac; only GLOBAL 429s it. D5 blocks the app.ts fix.
  - *narrowed:* Both failure modes hold as stated. Narrow one detail: RATE_LIMIT_FORCE is evaluated per-request inside `skip`, so only RATE_LIMIT_GLOBAL_MAX/RATE_LIMIT_COMPUTE_MAX must be set before createApp(); FORCE may be set any time before the request. Conclusion unchanged — a new test setting neither exercises no limiter and passes vacuously, and one that only asserts "eventually 429" pins the 300 global cap, not the intended 20 compute cap.
- **consequence** — refuted: `False`. Both mechanisms confirmed by probe. Compute limiter mounts only on /api/azodiac|/api/gemini; a non-azodiac /api POST survived COMPUTE_MAX=2. Without RATE_LIMIT_FORCE, COMPUTE_MAX=1 yields zero 429s. This changes TASK-007's new test file (must force the limiter and target the compute cap) and TASK-008's acceptance claim: its step 1 requires editing app.ts, which D5 forbids, so AC-010/REQ-S-005 is unachievable this run and must be reported unsatisfied.
  - *narrowed:* Sharper form: the actionable item is not a mount fix (D5 blocks app.ts). It is (1) write the TASK-007 rate-limit test to set RATE_LIMIT_FORCE=true, RATE_LIMIT_COMPUTE_MAX low and RATE_LIMIT_GLOBAL_MAX high before createApp() and assert the compute cap on the relationship route — it will fail, correctly; and (2) record AC-010/REQ-S-005 as explicitly NOT satisfied, since TASK-008 step 1 is unexecutable under D5.
- **coverage** — refuted: `False`. Both legs reproduce. app.ts:455 scopes the 20-cap to /api/azodiac|/api/gemini only; probe showed /api/relationships/western-synastry survives COMPUTE_MAX=2 and first 429s at the GLOBAL cap. Without RATE_LIMIT_FORCE, vitest.config.ts's DISABLE_RATE_LIMIT=true makes 10 POSTs return zero 429. No test, lint, type or CI mechanism exists: app.ratelimit.test.ts only probes already-covered paths, no ESLint config, and CI runs deploy only.
  - *narrowed:* One precision: RATE_LIMIT_FORCE is evaluated per-request inside skip(), not frozen at createApp() — only the numeric limits are read at construction. Both stated facts hold independently. Also, since D5 bars editing src/server/app.ts:455, REQ-S-005/AC-010 is unsatisfiable in this run by construction, so the finding should be stated as "cannot be satisfied", not merely "can be mis-tested".

---

## F-26 — [wired-in-prod] (refutes 0/3)

The command designated as the real-boundary evidence path structurally cannot produce real-boundary evidence: playwright.config.ts starts the app under test with FUFIRE_API_URL pointed at tests/e2e/mock-fufire.mjs and ENABLE_DEMO_PROFILES="true". RELATIONSHIP_REAL_BOUNDARY=1 is a runner-process env and does not override webServer.env. So `RELATIONSHIP_REAL_BOUNDARY=1 npm run e2e -- tests/e2e/relationship-real-boundary.spec.ts` runs against a mocked upstream with demo profiles enabled — precisely the three things AC-005/DOD-003/TASK-014 acceptance forbid. playwright.config.ts is named in neither the allowed-scope list nor the scope-stops table: it is an unnamed sixth composition point.

- **evidence** — refuted: `False`. All anchors verified. playwright.config.ts pins FUFIRE_API_URL to the mock and ENABLE_DEMO_PROFILES="true" in webServer.env; Playwright (runner/index.js:832) spreads config env after process.env, so it wins. npm run e2e is bare `playwright test`, so the designated command necessarily boots the mocked, demo-enabled app — violating AC-005, DOD-003 and TASK-014 acceptance. The file appears in no scope list.
  - *narrowed:* One phrase is loose: RELATIONSHIP_REAL_BOUNDARY=1 does reach the child process (process.env is spread in); what it cannot do is override the explicitly-pinned FUFIRE_API_URL/ENABLE_DEMO_PROFILES keys. Also, TASK-014 sits outside this run's D2 scope, so this is a latent defect in the designated evidence path rather than broken delivered code — it blocks the run from ever reaching real-boundary class without editing an unscoped sixth file.
- **consequence** — refuted: `False`. Technically confirmed (playwright/lib/runner/index.js:832-836: `...process.env, ...this._options.env`). Not purely TASK-014: D5's stated purpose is a bundled, complete release-boundary list, and this run's exit artifact is that list. A sixth composition point is missing from it. Leaving it out means the next agent runs the named command, gets green against a mock with demo profiles, and claims real-boundary evidence — the exact failure class the canvas names. Documenting it changes an acceptance claim in this run.
  - *narrowed:* Not a config fix (D5 forbids touching playwright.config.ts) and not the spec (TASK-014). The in-run action: add `playwright.config.ts` as a sixth row to the canvas Scope-Stops table (docs/ is in allowed scope, canvas §9, line 264) and to this run's limitation/handoff record — stating that `RELATIONSHIP_REAL_BOUNDARY=1 npm run e2e ...` is structurally mock-bound until that file is separately released, so credentials alone will not unblock TASK-014.
- **coverage** — refuted: `False`. No guard exists. Playwright merges `...process.env` before `...options.env` (node_modules/playwright/lib/runner/index.js:832-836), so playwright.config.ts:37,40 always force the mock URL and demo profiles. scripts/validate_pack.py:26 only string-matches the spec path in docs; deploy-cloudrun.yml has no e2e job; app.ts:476 exposes demoProfiles but nothing asserts it. Canvas 268-274 lists five scope-stops, not playwright.config.ts. Only prose mitigation exists.
  - *narrowed:* playwright.config.ts is an unnamed sixth existing-file composition point: TASK-014's validation command cannot satisfy its own acceptance criteria without modifying it, and no test, lint rule or CI check makes that visible. Narrowing: under D2 this run stops at TASK-013, so no false real-boundary evidence is emitted during this run — the defect is latent in the documented future evidence path rather than actively producing bad evidence now.

---

## F-27 — [wired-in-prod] (refutes 0/3)

RELATIONSHIP_MVP_ENABLED does not exist anywhere in the repo (grep over src, tests, config: zero hits) and, as named, cannot gate browser UI: Vite only exposes VITE_-prefixed vars to import.meta.env (the repo's own precedent is VITE_TAGESPULS_V2 at DailyPulse.tsx:31). It is additionally a build-time flag, which requires Dockerfile ARG/ENV lines to reach the deployed bundle — the Dockerfile's own comment states that without the ARGs the Railway var never reaches the Vite build. Dockerfile is in neither the allowed-scope list nor the scope-stops table. Consequence: ROLLBACK-001's promise 'disable to revert user exposure' is a rebuild-and-redeploy, not a toggle, and the flag as literally specified in five places gates nothing.

- **evidence** — refuted: `False`. All anchors verified. Grep: RELATIONSHIP_MVP_ENABLED has zero hits in src/, tests/, config/ — only four doc mentions. DailyPulse.tsx:31 reads `import.meta.env.VITE_TAGESPULS_V2` exactly as cited; vite.config.ts sets no envPrefix, so Vite's VITE_ default holds. Dockerfile carries the quoted comment ("Ohne diese ARGs erreichen die Railway-Vars den Vite-Build nie") and railway.toml builds via DOCKERFILE. Plan's "Allowed scope"/"Stop and ask if" never mention Dockerfile. ROLLBACK-001 quoted verbatim. Inference holds.
  - *narrowed:* Same finding with two precisions: the flag appears in four doc locations (plan:912, plan:1034, prd_report.md:279, prd_report.json:1798), not five; and the naming objection applies only to the browser half — a non-VITE name is valid for a server-side (process.env) gate in the BFF route, but ROLLBACK-001's "disable to revert user exposure" still is not a runtime toggle for any UI gating, since that needs VITE_ prefix plus Dockerfile ARG/ENV.
- **consequence** — refuted: `False`. TASK-012 step 3 ("additive feature-flagged navigation entry") is inside the 001-013 range, so acting changes this run. Verified: vite.config.ts sets no envPrefix, so the flag as named is undefined in import.meta.env; precedent is DailyPulse.tsx:31 / Dockerfile:23-24. Concrete actions in allowed NEW-FILE scope: create a VITE_-prefixed flag module plus a default-off test, and downgrade ROLLBACK-001 from satisfied to an owned limitation.
  - *narrowed:* Two separable actions. (1) In-scope now: any flag read in TASK-012's new files must be VITE_-prefixed via import.meta.env, default-off, in a new src/config module with a colocated test — the literal name RELATIONSHIP_MVP_ENABLED would silently evaluate undefined. (2) Out-of-scope, must be recorded not fixed: Dockerfile ARG/ENV is required for the flag to reach a deployed bundle, and Dockerfile is neither in D5's allowed scope nor the scope-stops table — add it there with an owner. ROLLBACK-001's "disable to revert user exposure" must be reported as unimplemented, not satisfied. Minor correction: the flag is named in four doc locations, not five (canvas:273 references flagging without naming it), and under D5 the route is unwired anyway, so the user-exposure risk is latent rather than live.
- **coverage** — refuted: `False`. No coverage exists. Zero hits in src/, tests/, .env.example, Dockerfile, vite.config.ts, CI. vite.config.ts sets no envPrefix, so Vite's VITE_ default applies (precedent DailyPulse.tsx:31). Dockerfile:16 states plainly that without ARGs the Railway var never reaches the Vite build; Dockerfile:23-24 shows each flag needs its own ARG — none exists here. src/server/runtime.ts validates only hosts. Even VITE_TAGESPULS_V2 has no test, so no mechanism could catch this.
  - *narrowed:* RELATIONSHIP_MVP_ENABLED is specified in 4 doc locations (not 5) and exists in no code, config, Dockerfile or CI. As named it could only gate server-side via process.env in src/server/app.ts; it cannot gate the browser navigation entry TASK-012 step 3 assigns it, because Vite exposes only VITE_-prefixed vars and vite.config.ts overrides no envPrefix. A working VITE_ variant would additionally need a new Dockerfile ARG/ENV pair, and the Cloud Run workflow passes no --build-arg at all. ROLLBACK-001's p0 promise to "disable to revert user exposure" is therefore a rebuild-and-redeploy, not a toggle, and is currently unimplementable as written. Nothing in the repo makes this visible.

---

## F-28 — [wired-in-prod] (refutes 0/3)

The App.tsx release is materially larger than 'an additive feature-flagged navigation entry'. renderTab() short-circuits at `if (!viewModel || !birthData)` BEFORE the tab switch, and PageShell disables every nav item with `disabled: !hasBirthData` where hasBirthData={!!viewModel}. A relationship entry added the naive way is disabled and unreachable until the user first completes the legacy anonymous InputForm -> FuFirE profile path — which directly contradicts JOURNEY-001/STORY-001/REQ-F-001, where the entry point is an authenticated stored profile and no prior chart computation is required.

- **evidence** — refuted: `False`. Every anchor verifies. App.tsx:117 guard precedes the switch at 137; PageShell:48-54 gate on !hasBirthData; App.tsx:160 passes !!viewModel; JOURNEY-001/STORY-001 (prd_report.md:122,128) and REQ-F-001 (throughline:159) require an authenticated stored profile with no prior computation; TASK-012 step 3 literally says "additive feature-flagged navigation entry". Methodik proves the point: undisabled, yet still blocked by the guard. Reachability needs control-flow restructuring, not addition.
  - *narrowed:* Identical, except "disables every nav item" should read "disables all seven content tabs"; input (PageShell.tsx:47) and methode (:55) carry no disabled flag. Methodik in fact strengthens the finding: it is enabled yet still blocked by the App.tsx:117 guard, showing the guard alone makes a naively added relationship tab unreachable.
- **consequence** — refuted: `False`. Verified: App.tsx:117 short-circuits before the switch, PageShell.tsx:48-54 disables via hasBirthData={!!viewModel}. Two live consequences inside TASK-001..013, both new-file-only: (1) ProfileStep/RelationshipEntry must be designed and tested to mount with zero legacy viewModel/birthData props, resolving the owned profile via /api/me/profiles — that test is writable today and src/components/relationship/ is still empty; (2) TASK-012's acceptance claim must drop "additive navigation entry" and record the deferred gating refactor honestly.
  - *narrowed:* Drop the "App.tsx release" framing (out of scope under D5). Actionable now: new relationship components must take no ProfileViewModel/BirthData props and must have a colocated test asserting the flow reaches profile selection with no legacy chart state; and TASK-012 must be re-scoped to a self-contained, unwired flow whose residual wiring cost is documented as a gating refactor, not a nav item.
- **coverage** — refuted: `False`. Verified and uncovered. App.tsx:117 short-circuits before the tab switch; PageShell.tsx:48-54 gates every item on hasBirthData=!!viewModel (App.tsx:160). No App.tsx or PageShell test exists. The only related assertion, tests/e2e/fufire-integration.spec.ts:37, asserts the gate holds — locking the behavior in, not catching it. deploy-cloudrun.yml runs no tests or lint. No type or lint rule reaches renderTab's short-circuit; no prose mitigation either.
  - *narrowed:* The App.tsx/PageShell release is materially larger than "an additive feature-flagged navigation entry" (plan line 822). renderTab() short-circuits at App.tsx:117 (!viewModel || !birthData) before the switch at line 137, and also at line 74 (loading) and line 91 (errorMsg); PageShell.tsx:48-54 disables nav items via hasBirthData={!!viewModel} (App.tsx:160). So a naively-added relationship entry is both disabled and unreachable until a ProfileViewModel has been successfully compiled (App.tsx:42) — contradicting REQ-F-001, which needs only personAProfileId. Narrower than stated: an authenticated user with a saved profile can satisfy the gate via AccountMenu.tsx:220 -> App.tsx:150 without InputForm, so the precondition is "a successful FuFirE profile compile", not specifically "the legacy anonymous InputForm path". Broader in one respect: a failed compile routes the relationship tab to the profile error screen at line 91.

---

## F-29 — [wired-in-prod] (refutes 0/3)

TASK-013's contrast evidence is decoupled from whether the stylesheet is ever loaded. src/styles/relationship.css is in the allowed scope, but today CSS reaches the app only through src/index.css, imported once at main.tsx:4 — and index.css is out of scope. src/__tests__/relationshipContrast.test.ts (also allowed scope) computes ratios from token values and passes identically whether the stylesheet is loaded or orphaned, so REQ-NF-001/NF-002 and TASK-013 acceptance would be reported satisfied while the running app renders unstyled. The canvas lists index.css as a required scope-stop, but a clean in-scope escape exists that it never names: a component-level `import "../../styles/relationship.css"` inside a new .tsx file works under Vite and needs no existing file.

- **evidence** — refuted: `False`. All anchors verified. main.tsx:4 is the only .css import in src/; index.css is the only stylesheet. Canvas:256/258 allow relationship.css and relationshipContrast.test.ts; :274 makes index.css a hard stop under D5. Plan:856 validates TASK-013 solely via that token test plus lint, and precedent contrast.test.ts uses hardcoded hex with no CSS loading — so it passes identically when orphaned. The component-level Vite import escape is real and unnamed.
  - *narrowed:* TASK-013's acceptance ("core text/control pairs meet the agreed AA threshold") can be declared satisfied by a token-value test that never proves src/styles/relationship.css is loaded, since index.css (the only import path, main.tsx:4) is out of scope. Attribute this to TASK-013 acceptance rather than REQ-NF-001/NF-002, which are viewport and keyboard requirements verified by Playwright/axe. Fix: require the stylesheet be imported from a new in-scope component under src/components/relationship/ — a route the canvas never names.
- **consequence** — refuted: `False`. Verified: main.tsx:4 is the only CSS import in the repo; src/styles/ does not exist. TASK-013 routes loading through out-of-scope index.css, and its test mirrors contrast.test.ts — pure hex arithmetic via contrastRatio(), green whether the stylesheet is loaded or orphaned. Acting changes files and tests inside TASK-001..013: import relationship.css from a new in-scope component (removing a planned scope-stop) and add a wiring assertion so acceptance is not claimed on token math alone.
  - *narrowed:* Narrower defensible form: the "running app renders unstyled" consequence is moot at production level, since D5 blocks src/server/app.ts and the route is never reachable in prod regardless. The live defect is evidentiary and internal to the run: TASK-013 acceptance would be reported satisfied at evidence class 'integration' with zero evidence that relationship.css is reachable from any component. Also, a jsdom contrast test cannot measure real rendered contrast from a Vite-processed stylesheet, so the fix is (a) component-level `import "../../styles/relationship.css"` in a new src/components/relationship/*.tsx file plus (b) a static assertion that this import exists — not a rendered-contrast test.
- **coverage** — refuted: `False`. Not covered. `npm run lint` is only `tsc --noEmit` (package.json:11); I empirically confirmed it exits 0 both with and without a component-level CSS import. Zero `getComputedStyle`/`toHaveCSS` assertions exist anywhere in src/ or tests/. No ESLint, stylelint, knip or depcheck. CI (.github/workflows/deploy-cloudrun.yml) runs no tests or build gate. contrast.test.ts is pure hex arithmetic — the identical blind shape, precedent not coverage. Canvas:274 names only index.css.
  - *narrowed:* Narrow the consequence clause. Under D5 the flow never reaches production at all, so "the running app renders unstyled" is not the live harm. The defensible version: TASK-013's acceptance evidence cannot distinguish a wired stylesheet from an orphaned one, so REQ-NF-001/NF-002 get recorded green on token arithmetic alone, and that stale green carries forward into the later, separately-released index.css wiring where nobody re-verifies. Two supporting facts strengthen it: (1) the component-level `import "../../styles/relationship.css"` escape is verified real — tsc exits 0 — so the canvas scope-stop at line 274, which names only src/index.css, does not actually force the stop it believes it forces; (2) REQ-NF-001 and REQ-NF-002 are defined in plan:179-180 as viewport-scroll-free at 320/768/1440 and keyboard-operable with visible focus — neither is a contrast property, so relationshipContrast.test.ts does not exercise the requirements it is mapped to even before the loading question arises.

---

## F-30 — [wired-in-prod] (refutes 0/3)

Within the D2 run scope there is not a single acceptance criterion in either artifact that fails when the capability is unreachable. Every AC reachable in TASK-001..013 (PRD AC-001..004, AC-006, AC-007, AC-009, AC-012; plan AC-001..006, AC-008, AC-009) is satisfiable by process-local doubles. The only ACs that would catch F1-F5 are AC-005/AC-010 and PRD DOD-002/DOD-003, and all of them sit in TASK-014/015, which D2 defers. The run therefore terminates with every in-scope AC green and zero signal about composition — the honest label 'evidence class integration' is carried by prose in the canvas rather than by any failing artifact.

- **evidence** — refuted: `False`. Every anchor checks out. PRD:232-243 and plan:194-264 match the enumeration exactly; TEST-004 literally says "process-local upstream double"; TASK-012/013 validate via colocated component tests only; AC-005/DOD-002 sit in TASK-014/015 (PRD:316, EVD-008 `missing`). "Evidenzklasse integration" exists only as canvas prose (161, 328) — traceability.md has no western-synastry row and package.json has no gate script. Nothing mechanical fails.
  - *narrowed:* Same claim, with one correction: AC-010 (rate limit) is NOT deferred to TASK-014/015 — PRD:314 and plan TASK-007 step 5 / TASK-008 step 1 place it inside the D2 window. It still cannot catch the unreachability, because the canvas (~line 283) reroutes the relationship rate-limit proof into the new file `src/server/app.relationship.test.ts` (since `app.ts` and `app.ratelimit.test.ts` are D5-barred), which must compose its own Express app. So AC-010 moves into the "satisfiable by process-local doubles" set, making the in-scope double-satisfiable set larger than the finding states. The only ACs that would catch F1-F5 are PRD AC-005 / plan AC-007 plus DOD-002/DOD-003, all in TASK-014/015.
- **consequence** — refuted: `False`. Acting on it changes real artifacts. app.ts:455 limits only /api/azodiac and /api/gemini, so AC-010 can only pass on a test-local mount — a false green in TASK-007. TASK-012 is titled "end-to-end" and lists App.tsx/PageShell.tsx as Modify, both out of scope, yet its ACs stay satisfiable. Needs a new-file composition guard plus restated TASK-007/TASK-012 acceptance wording.
  - *narrowed:* Not every in-scope AC is vacuous, but the run must (a) add a new-file guard asserting /api/relationships is absent from app.ts routes and the compute-limiter list, and (b) restate TASK-007's AC-010 claim and TASK-012's "end-to-end" claim as process-local/unwired.
- **coverage** — refuted: `False`. No covering mechanism exists. .github/workflows/deploy-cloudrun.yml is the only workflow and runs neither test nor lint. src/server/app.ts:445-967 registers routes inline; no route-inventory or unmounted-404 test exists in app.test.ts. package.json:11 lint is bare `tsc --noEmit` — no knip/ts-prune, so an unimported router is invisible. scripts/validate_pack.py only greps doc strings. Canvas:160-161,327 is prose, which Lens 3 excludes.
  - *narrowed:* Substance holds, with one numbering fix: PRD AC-010 traces to TASK-007/008 (in scope), not only TASK-014/015 (prd_report.md:314,322). But under D5 the compute limiter at src/server/app.ts:455 is frozen, so in-scope AC-010 is still only provable against a test-local Express app. The composition-catching ACs remain PRD AC-005 / plan AC-007 and DOD-002/003, all deferred.

---

